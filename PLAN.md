# Reckless Driving — Development Plan

## Workspace / environment (added 2026-09-16)
Development moved into a Coder workspace (`adamromatowski/game-dev`).
- Game file: `~/game/carCrash.html`; docs alongside it; `assets/` for art.
- Live preview: port 8080 serves `~/game/` — open `/carCrash.html`, just
  refresh after an edit. Started with `python3 -m http.server 8080`.
- File uploads: port 8090 drag-and-drop page (`tools/upload_server.py`).
  Needed because chat attachments reject `.html`. Files land in
  `incoming/`, or `assets/` with the checkbox ticked.
- Git repo initialised here — changes are committed per working state, so
  bad edits can be rolled back (new capability vs. the old workflow).
- `AGENTS.md` added: standing rules auto-loaded by the agent (single-file
  constraint, settled decisions not to re-litigate, changelog format).
- Node v22.23.2 and Python 3.14.4 available in the workspace — relevant to
  the packaging and leaderboard-server items below, both of which can now
  actually be BUILT here rather than only planned.

## Status legend
- [ ] Not started
- [~] In progress
- [x] Done

Completed batches are no longer listed here — the full history (what changed, why, and how it was verified) lives in `CHANGELOG.md`. This file is a forward-looking backlog only.

---

## MAIN MENU REDESIGN — DONE (2026-09-20, Batch 426)
Ported from `Main Menu Options.dc.html` TURN 5 (3b), with the level badge kept as the game's own ring and START RUN cycling colour continuously, both by direct request. LANES/DIFFICULTY now live on the menu (closing the older "move them out of Settings" item) but still write to the existing selects, so game logic is untouched. See CHANGELOG Batch 426.
- [x] **SETTLED (Batch 427): AWARDS and STATS, everywhere.** Choice was delegated to me. AWARDS beat BADGES (matches the supplied design, fits the 9px tile, and the bronze->amethyst ladder is a medal metaphor); STATS beat STATISTICS because the 3-column grid cannot carry the long word, which partly reverses Batch 414 - that decision was made under the old wide two-column menu. Internal ids/functions/localStorage keys unchanged. Original note: the design labels two tabs **STATS** and **AWARDS**. Batch 414 renamed STATS -> STATISTICS by explicit request, and the ACHIEVEMENTS button has been called that since Batch 180. The short labels are in because the 3-column grid is narrow, but this does partly undo an earlier decision — say which wins. The screens' own headings still read "Statistics" and "Achievements"; only the menu tiles are short.
- [x] **Checked (Batch 427): kept in both places, and they cannot diverge** - the menu writes to the same `#selectLanes`/`#selectDifficulty` the Settings UI reads. Verified: setting 8 lanes on the menu leaves Settings reading `1.03x MULT`. Original note: **Settings screen now duplicates lanes/difficulty.** They are still there and still work (the menu writes to those same controls). Decide whether to remove them from Settings or leave both.
- [x] **SUPERSEDED (2026-09-20, Batch 441)** — a dedicated `Icon Set FINAL.dc.html` settled the picks; six icons were replaced from it and two were already identical. Original: The doc's TURN 4 offered three icon candidates each for SCORES / GIFT / SETTINGS / MISSIONS. TURN 5's picks were taken as final since that is the newest turn — worth a look in case a different candidate was preferred.
- [ ] Old menu CSS (`.menu-actions`, `.menu-title`, `.level-badge` menu variant, `.btn-row` in the menu) is now unused by the menu but still used by other screens in places — not cleaned up, flagged rather than risked.

## TUTORIAL — still owed from the 2026-09-20 batch
- [x] The mocked main-menu screen is gone; the card now explains each tab with the game's wordmark as its art (Batch 426).
- [x] **DONE (Batch 427)** - THE MAIN MENU moved out of PROGRESSION into BEGINNER, directly after THE WHEEL, so the order is drive -> navigate -> everything else. SPOTTING CARS stays in PROGRESSION as asked. Original note: **Re-order for a new player.** Direct note: the most important thing first is how you steer and move, then what the tabs do, and "spotting cars I don't think it should be in the beginner". Today the order is BEGINNER (wheel, dying, scoring, jump, paid) -> INTERMEDIATE -> MASTER -> PROGRESSION (menu, spotting, level, dailies, performance) -> ABOUT. Needs a decision on the target order: my suggestion is to move THE MAIN MENU up to just after THE WHEEL (a new player needs it immediately), leave SPOTTING CARS where it is in PROGRESSION (it is already not in BEGINNER), and otherwise keep the difficulty ramp intact.

- [ ] **Cones are knock-aside only (Batch 427).** Driving through them throws them clear; they carry no hitbox, no score and no energy. If they should ever actually *slow* the player, note the reasoning against it: they sit in front of a car that already ends the run, so a damaging cone stretches one obstacle across two lanes' worth of stopping distance.

- [ ] **Watch: NPC crashes may now be too rare for the `witness_crash` mission (Batch 431).** Measured after the change: 0 crashes in 5 simulated minutes at 4 lanes/RECKLESS, 1-3 at 9 lanes/SUICIDAL. That is the requested rarity, but the daily mission asks you to witness one. `NPC_INATTENTIVE_CHANCE` (currently 0.10) is the single number that tunes it; alternatively the mission could be re-scoped.

## BOX-ONLY CARS — REVERSED (2026-09-20, Batch 422)
Direct decision: **every existing car is now an ordinary purchase** — found in traffic, bought with coins at its real (already-researched, Batch 345) price. The 13 formerly box-exclusive cars are no longer locked. `boxOnly` now means the opposite of what it used to: it marks the cars a box *may award*, and nothing carries it, so the car tier is dropped from the reward roll (and from the DROP CHANCES panel) until something does. See CHANGELOG Batch 422.

The previous decision (Batch 406, letting box-exclusives spawn in traffic but stay unpurchasable) is superseded. The dead `GARAGE_NPC_RARITY` map is still present and unused — its guard now also references a flag no car carries, so it is doubly stale; still harmless, still a cleanup-pass candidate.

- [x] **DONE (2026-09-21, Batch 443) — all three shipped as `boxOnly: true`, and the prediction held: no code change was needed, the whole box system switched back on by itself.** The three open decisions were settled as: `rarity: legendary` for all (which does NOT spawn them, since the spawn loop skips boxOnly - it only sets Garage grouping and duplicate-refund rate), sizes BICYCLE h20/w8, FISH h22/w12, COUCH h18/w20 (multipliers x0.90 / x0.97 / x1.00, all inside the existing spread), and no `unlock` price at all since a box-exclusive never shows one. One bug fell out of it - see the CHANGELOG on `extraBoxCanStillGive()`. Original: **Next: the fun/joke vehicles** — user's own plan, named examples **bicycle, fish, couch**, "stupid but maybe fun". These become the new box-exclusives: add the sprite, give the car `boxOnly: true`, and the whole box system (rarity-weighted roll, duplicate compensation, mystery tile, LOCKED state, COLLECTOR's amethyst tier, the DROP CHANCES car row) switches back on by itself — verified at runtime, no code change required. Decisions still to make when they are added: whether each gets a `rarity` (which is also what makes it spawn in traffic — a fish in traffic may be funny or may be too silly), what `h`/`hitboxW` they use, and whether they get an `unlock` price at all (a box-exclusive's price is never shown, so it can be left off).
- [x] **PARTLY DONE (2026-09-21, Batch 445) — 6 of the 11 shipped**, drawn by the user and supplied as `Reckless Vehicles v4.dc.html`: GRAND PIANO, GIANT SNAIL, SHIP, BUMPER CAR, CLAW-FOOT BATHTUB and GIANT DONUT, all transcribed pixel-exact from the doc. **The remaining 5 are DROPPED (2026-09-21, direct decision: "as for the rest undrawn joke cars ignore them because I won't be doing them") - shopping trolley, office chair, roller skate, hot dog cart, toaster, wheelbarrow and bed are not happening.** The constraints below still apply to those, with one correction learned from the transcription: a doc that supplies exact SVG paths does NOT need `sil53()`, because its outline is already baked into the art. Original: **Joke-vehicle shortlist, to be drawn by a separate agent (2026-09-21).** Eleven candidates, picked for being hard to read at 14-22px top-down - the angle at which most objects collapse into a rectangle. **GRAND PIANO** (bent side, no straight edges; lid, harp, strings, 3 castors, keyboard), **SHOPPING TROLLEY** (wire mesh, 4 castors at 4 angles, child seat), **GIANT SNAIL** (spiral shell - the classic pixel trap - eyestalks, slime trail), **OFFICE CHAIR** (5-star base, radial symmetry that will not align to the grid), **SHIP** (deck, masts, rigging, sails, railings, lifeboats - the busiest silhouette of the set), **CLAW-FOOT BATHTUB** (oval, 4 clawed feet, taps, water with a reflection), **GIANT ROLLER SKATE** (laces and eyelets), **HOT DOG CART** (parasol from directly overhead = a panelled circle), **TOASTER** (chrome is the problem, not the shape), **WHEELBARROW** (one front wheel, 2 trailing handles, heaped tray), **BED** (pillows, duvet folds, headboard, castors). **Constraints the drawing agent must be given:** top-down with the nose UP; width = the car's `hitboxW`, centred on x=12 so the left edge is `carLocalX53(w)`; height = `h`, typically 18-34; outline comes from `sil53(ctx, spans)` with spans as `[y, x, width]`, never hand-drawn; `c` is the player's paint so every vehicle needs a real painted surface or it looks broken in the Garage, with `shade(c, +/-n)` for lighting; canvas `fillRect` only, no images or paths; and do NOT build on `bodyShell53`/`bodyShellW53`, which draw a car complete with headlights and a windscreen. **Two lessons from drawing the first three (Batch 443):** contrast does the work at this scale (the couch read as a striped box until its arms went to -.38 against the cushions), and any coloured detail thinner than ~3px vanishes (the bicycle's 2px frame was invisible until widened to 4).
- [x] **RESOLVED (2026-09-21, Batch 446) — SHIP keeps x1.88.** The question was whether it is genuinely harder to drive. Measured: it is NOT longer than Road Train (both h60), it is WIDER - hitboxW 22 vs 14, so 57% more car to fit through every gap, and hitboxW drives real collisions. Hardest vehicle in the game to place, so the highest multiplier stands. Original: **Decision left open (Batch 445): SHIP sits at x1.88**, the highest multiplier in the game - past Road Train's x1.79 and past the x1.82 ceiling lane x mode can reach together. Batch 440 deliberately kept the best car under that ceiling so no single lever outweighed the other two combined. Kept as-is for now because it follows the direct "longer vehicles earn more" instruction and the SHIP cannot be bought at any price, but flagged in chat with an offer to trim it to x1.79.
- [ ] **Abilities for the 4 ability-free joke vehicles — open by the user's own note** ("for now I don't have the idea for the rest of the abilities, but maybe I will come up with something"). GRAND PIANO, GIANT SNAIL, BATHTUB and GIANT DONUT currently run `noAbility: true` with a coin bonus instead (Batch 446). If any of them later gets a real ability, drop its `coinBonus` at the same time - the bonus exists precisely because there is no ability, and keeping both would make it strictly the best car in the game.
- [ ] **Coin-bonus numbers are a judgement call, not a spec (Batch 446).** DONUT +75%, PIANO +65%, BATHTUB +55%, SNAIL +40%; SHIP and BUMPER CAR none, because they have an ability instead. The brief was only "some should have more and some should have less". The floor is deliberately high because jump is a survival tool rather than flavour, so a small bonus would never be worth giving it up. Retune freely if they play wrong. (Batch 447 removed BICYCLE/FISH/COUCH, which also carried bonuses.)
- [ ] **Integer scaling end to end — the real fix for sprite fidelity (Batch 469).** CAR_SCALE is 1.3, and resampling pixel art by a fractional factor can only blur it or deform it; there is no third outcome. Measured: at 1.3x nearest-neighbour, 22 of 32 sprite rows draw single-height and 10 draw double, so outlines land 1px in places and 2px in others and circles gain flat sides. Smoothing (current) keeps shapes but softens edges. The fix is to author the world at 1x - CAR_SCALE 1, every raw pixel constant divided by 1.3 (CANVAS_HEIGHT 260->200, PLAYER_MIN_Y, MAX_FLIGHT_HEIGHT, spawn margins, trail/air lengths) - and let ONE integer display scale enlarge the finished frame, with letterboxing so that scale stays whole. Real refactor with real regression risk; not attempted mid-batch.
- [x] **FIXED (2026-09-22, Batch 461) - sprites render 1:1 into an offscreen buffer and blit nearest-neighbour; back to 10 distinct colours from 182.** Original diagnosis: Direct report of a "strange grey shadow / overlay" and blur on cars in gameplay but not in the Garage. Root cause measured: CAR_SCALE is **1.3**, so drawScaledVehicle() puts every fillRect edge between device pixels and canvas antialiases it. One sprite drawn at gameplay scale has **182 distinct colours**; the same sprite at an integer scale has **10**. Fixing it means rendering each vehicle to an offscreen buffer at 1:1 and blitting with imageSmoothingEnabled=false - which needs all 11 drawScaledVehicle() call sites to pass a context into their drawFn (they currently close over ctx/gctx/menuCtx), plus padding for things drawn outside the sprite box (thruster flames, shield-bump guard, jump shadow at up to MAX_FLIGHT_HEIGHT). Contained but not small, and it touches every vehicle on screen. Offered, not done.
- [x] **WRONG, and removed (2026-09-21, Batch 455).** This entry claimed a trail behind the player was impossible. It was based on measuring the player at REST (y 217 of 260) and generalising from that one number - the player drives forward to PLAYER_MIN_Y 40, giving 177px of range and up to 178px of open road behind. The GIANT SNAIL trail is built and works. Kept as a reminder not to conclude "impossible" from a single measurement of one state.
  - **Confirmed constraints (2026-09-20, direct)**: joke cars do **NOT** spawn in traffic and are **not discoverable** — box only. They get their own Garage section (working name "SPECIAL"), separate from the rarity groups. They will mostly be slow/weak, and the user wants **one shared special ability none of the normal cars have** — explicitly worried about it landing either busted or underwhelming. Design discussion held; not yet decided.
  - **Implementation gotcha found while discussing this**: the traffic spawn pool is `GARAGE_CARS.filter(c => c.rarity)` (carCrash.html ~8355), so *anything* with a rarity spawns. But the box roll picks by rarity too (`c.rarity === rarityPick`), so joke cars need a rarity to be rollable. The two requirements collide. Fix is one condition — `filter(c => c.rarity && !c.boxOnly)` — which is exactly the guard the now-dead `GARAGE_NPC_RARITY` line already uses, so it is precedented, not new design.
  - **Balance finding — score is self-limiting, measured not guessed.** A run starts at exactly **50 km/h**, which is precisely where the speed bonus begins (`+1%` per km/h above 50). So a joke car capped below 50 earns **zero** speed bonus for the whole run and is stuck at its base multiplier. Measured at best settings (3 lanes + SUICIDAL): a bicycle-sized car (h16/w8) reaches **2.25x** at 40 km/h and 2.47x at 60, against **5.21x** for a Stock car at 170 and **7.51x** for a Road Train at its top speed. Joke cars therefore sit at roughly a **third** of a real car's scoring rate before accounting for the slower road scroll (fewer cars passed per minute), which widens the gap further. **Conclusion: no ability can make a slow joke car the optimal scoring choice, so the ability can afford to feel generous.**
  - **The real busted-risk is NOT score — it is the count/time ladders**, which have no multiplier to cap them: STAYING ALIVE (longest single-run survival), OVERTAKER (lifetime passes), TOO CLOSE TO COMFORT (lifetime close calls), HIGH FLYER, ROAD RAGE, plus the missions `survive_seconds`, `pass_cars`, `close_calls`, `airborne_seconds`, `use_bars`, `change_lanes`. A slow car is **already** easier to survive in (lower closing speed), so any ability that further extends survival — invulnerability, cheat-death, auto-dodge, time-slow — compounds an advantage that already exists and trivialises those ladders. **Design rule that follows: prefer an ability that raises risk rather than lowering it.**
  - **Recommended shape: a speed burst** ("pedal furiously" / "backfire" / "flop"), spending the existing energy bar like Jump and Ram do. It fixes exactly the axis these cars are weak on (low speed ⇒ low multiplier), it *increases* danger while active so it cannot inflate the survival/count ladders, it is tunable with two numbers (magnitude, duration), and it is a genuinely third ability category — Jump grants immunity, Ram destroys, this grants neither, it is purely offensive on the score axis. Not-busted bound, measured: even boosted to 130 km/h a bicycle reaches only **4.05x**, still under a plain Stock car at 170 (5.21x). Cap the boost below **250 km/h** regardless, or it trivialises the I AM SPEED achievement.
  - Alternatives considered, each with its specific risk: **shrink the hitbox** (fits bicycle/fish, but it is survivability ⇒ STAYING ALIVE risk); **honk/scatter traffic** (very funny for a couch, but risks farming close calls); **time-slow** (thematic irony, but pure survivability, worst ladder risk); **coin-doubling burst** (harmless to score ladders but feeds BUSINESSMAN); **cosmetic-only** (zero risk, but this is the "underwhelming" outcome the user explicitly wants to avoid).
- [x] **FIXED (2026-09-20, Batch 440)** — boxes now also require that they can still GIVE something (an unowned gift paint or a box-exclusive car), so they hide rather than sell a guaranteed loss. Original: **Extra Boxes now need all 52 cars owned to appear** (was 39), because `ownsAllPurchasables()` gates them and every car is purchasable now. Until the joke cars exist those boxes pay only coins/XP/paint. Worth revisiting when the new cars land — if the bar feels too high, that gate is the thing to change.

---

## PACKAGING — REVISED 2026-09-20 after reading the user's own Lockdown project

**The Tauri recommendation below is superseded for the Windows build.** Direct instruction was to look at how `C:\Users\adam\Documents\Visual Studio Code\Lockdown` does it — it is the same user's project and already has a working install/update/uninstall pipeline. Scope narrowed by the same message: **Windows only for now.** Built 2026-09-20, see STATUS below.

**How Lockdown does it** (`scripts/build.ps1`, `installer/setup.py`): PyInstaller builds the app folder → a self-test runs the built exe and fails the build if it doesn't report OK → the folder is zipped to `payload.zip` → a second PyInstaller pass builds `LockdownSetup.exe` (`--onefile --noconsole --uac-admin`) with that zip embedded. `setup.py` is a tkinter window that installs, updates and uninstalls. **The update flow the user described**: the same Setup.exe reads `DisplayVersion` from the `Uninstall\Lockdown` registry key, and if an install exists the button says "Update" instead of "Install" — so an update is just a newer Setup.exe to double-click. User data lives in `C:\ProgramData\Lockdown`, never in the program folder, so an update never touches it.

**Proposed shape for Car Crash**: the same two-pass build, with the app being a ~30-line Python WebView2 host (pywebview) that opens the bundled `carCrash.html`. Rust/Node not needed. Confirmed available 2026-09-20: `Lockdown\.venv` has Python 3.13.7 + PyInstaller 6.22.3; WebView2 runtime 153.x is installed. Differences from Lockdown, all simplifications: no Windows service, no watchdog, no admin — so it can install per-user into `%LOCALAPPDATA%` and skip the UAC prompt entirely.

**STATUS: BUILT (2026-09-20, Batch 421).** `scripts\build.ps1` → `build\RecklessDrivingSetup.exe` (23 MB), per-user, no admin, install/update/uninstall from one exe. Both blockers below were fixed first. Remaining on this front:
- [ ] **Unsigned-binary warning still applies** — SmartScreen will warn on first run, as the 2026-09-11 research below describes. Nothing was done about it; the mitigations listed there (ship with a README note, or a code-signing cert) are still the options.
- [ ] **Fixed port 42017.** The save origin depends on it, so if something else is using that port the game's saves would be under a different origin. Not hit in testing, no handling written — flagged in case it ever shows up.
- [ ] Not started: Linux/macOS (deliberately out of scope, direct instruction "for now only Windows") and Android/Capacitor.

**The two blockers, both now fixed:**
- [x] **Fonts break offline — FIXED (Batch 421)** via `tools/embed_fonts.py`: Silkscreen/DotGothic16/VT323 are now base64 `@font-face` inside the file (68 KB), `unicode-range` preserved so missing glyphs still fall back instead of showing tofu, latin + latin-ext subsets only. `build.ps1` refuses to build if a Google Fonts reference reappears.
- [x] **Save data — FIXED (Batch 421)**, and it needed three overrides, not one: pywebview's `private_mode` defaults to True (WebView2 InPrivate — localStorage never written — *and* the user-data folder deleted on close), so `private_mode=False`; `storage_path` pinned to `%LOCALAPPDATA%\RecklessDriving`, outside the program folder the update replaces; and a fixed http port, because pywebview serves local files from `127.0.0.1:<port>` and localStorage is keyed to that origin. Proven across two processes through the game's own `saveAllTimeStats()`. **This also closes the "Save-data continuity across app updates" item below, for Windows.**
- [x] **DONE (2026-09-21) — `git init` run, initial commit 06bd777 covers the whole project at 2.228.2.** The .gitignore that was already waiting excludes .venv/ and build/, both reproducible from source. Original: **Still open: this folder is not a git repo** (Lockdown is), so there is no rollback if a build step corrupts `carCrash.html`. A `.gitignore` is in place ready for it, and `backup/carCrash.2.214.0.pre-packaging.html` is the manual safety copy taken before Batch 421 — but that is a one-off, not a substitute. Offered, not done.

## CROSS-PLATFORM APP PACKAGING — goal stated 2026-09-11, Tauri research below superseded for Windows (see above)

User's stated goal, verbatim intent: within "a few days," turn this into a real app running on **Android, Windows, Linux, and macOS**. Explicit constraints: doesn't need to be an "official" polished installer for most platforms; does NOT need the user to spin up a localhost/dev server or any manual setup — should run from essentially one command or one double-click; should be reasonably safe (not a random unsigned installer people have to fight their OS to run). Asked for options to be researched and saved here — **research only below, nothing implemented yet.**

**Researched options (2026-09-11):**

- **Desktop (Windows/Linux/macOS) — recommend Tauri.** Wraps this single HTML file in the OS's own native webview, producing one real binary per platform (.exe / .AppImage / .app) that just runs on double-click — no browser, no localhost, no install wizard required unless one is deliberately built. Tauri bundles are ~2-10MB vs. Electron's ~80-200MB for the same app, uses meaningfully less idle memory, and multiple current (Sept 2026) comparisons call it "the recommended choice for new desktop applications in 2026." Tradeoff: Tauri's build tooling is Rust-based (a Rust toolchain has to be installed to BUILD it, though the output binary needs nothing extra to RUN) — a real but one-time setup cost, not a per-user cost.
  - **Electron** is the fallback if Tauri's Rust toolchain becomes a real blocker — much larger community/tooling maturity, but 10-20x the bundle size for the same result. Only worth it if Tauri specifically causes trouble.
- **Android — recommend Capacitor.** The standard, actively-maintained, free path from a plain HTML/CSS/JS project to a real installable `.apk`: wraps the page in a native Android WebView shell. Produces a real APK the user can sideload (install directly, no Play Store needed) — matches "doesn't need to be super official." Alternative: one-click online HTML→APK converters (e.g. Capsule HTML2APK, html2app) exist for near-zero setup, but that means uploading the game to a third-party service — worth avoiding unless Capacitor itself proves troublesome.
- **Not recommended as the primary path, but worth knowing about**: a PWA (`manifest.json` + a small service worker) would make the game installable straight from a browser on Android AND desktop Chrome/Edge with near-zero extra work — but it fundamentally needs the game reachable at a URL (hosted somewhere), which conflicts with the "no localhost/server setup" requirement as stated. Worth revisiting only if hosting it somewhere stable ever becomes acceptable.

**Recommended path once picked up**: Tauri for Windows/Linux/macOS (one shared build config covers all 3), Capacitor for Android — 2 build pipelines total, both feeding off this exact same `carCrash.html` with no rewrite needed, matching "keep it a single HTML file" from the Notes & Constraints section below. Both produce a locally-runnable artifact (native binary / APK) with no server, no localhost, no ongoing hosting cost.

Sources: [Tauri vs Electron 2026](https://tech-insider.org/tauri-vs-electron-2026/), [Tauri vs Electron vs Neutralino 2026](https://www.pkgpulse.com/guides/tauri-vs-electron-vs-neutralino-desktop-apps-javascript-2026), [Best Desktop App Frameworks 2026](https://www.pkgpulse.com/guides/best-desktop-app-frameworks-2026), [Capacitor HTML5 games](https://dev.to/eonarheim/android-games-with-capacitor-and-javascript-fac), [Capsule HTML2APK](https://capsuleteam.itch.io/capsule-html2apk-html-to-apk-converter)

**Trust/signing follow-up (2026-09-11), answered directly**: every unsigned build from the path above WILL trigger an OS-level "this might not be safe" warning by default — Windows SmartScreen ("Windows protected your PC," defaults to "Don't run"), macOS Gatekeeper ("from an unidentified developer," blocks the double-click), Android ("This type of file can harm your device" on installing an APK outside the Play Store). Linux/.AppImage is the mildest (just needs `chmod +x`, no scare dialog). None of this means the file IS unsafe — these are reputation checks, not malware scans, and ANY brand-new unsigned indie binary triggers them regardless of code quality. Very normal for indie/hobby games (itch.io is full of it), but it will cost some fraction of more cautious players. Mitigations, cheapest to most effort: (1) free — ship via itch.io/GitHub with a README explaining the warning upfront; (2) ~$100-200/yr Windows code-signing cert, softens SmartScreen; (3) $25 one-time Play Store listing, removes Android's warning almost entirely; (4) $99/yr Apple Developer Program + notarization, removes the Gatekeeper block; (5) a PWA install sidesteps this ENTIRELY (goes through the browser's own native install prompt, no separate executable, no OS gatekeeping) — the one place PWA has a real edge over native builds, at the cost of still needing the game hosted at a URL.

**Direct follow-up — a maximally-transparent local-server option**, user's own idea: instead of (or alongside) a compiled binary, give the user a way to run the game themselves via something fully readable/auditable rather than an opaque .exe:
- **Option A (recommended default)** — pure stdlib, zero custom code: `python -m http.server 8000` (one command, using ONLY Python's own standard library — nothing of the dev's to trust beyond the plain-text game file itself), then open `http://localhost:8000/carCrash.html`. Maximum transparency; the tradeoff is requiring Python already installed.
- **Option B** — a tiny `run.py` (~10 lines) that starts the same server AND auto-opens the browser to the right URL, for people who want closer to double-click convenience. Still fully readable in a text editor, just no longer pure-stdlib-only.
- **Option C** — a `.bat`/`.sh` wrapper per OS that calls the same `python -m http.server` command, giving genuine double-click-and-go on each platform while staying plain-text/inspectable (unlike a compiled binary).
Recommended combination: lead with Option A in the instructions (safest, clearest), offer Option C as an optional convenience layer. This is a complementary THIRD distribution path alongside Tauri/Capacitor above — a good "don't trust the .exe? here's the transparent alternative" fallback — not a replacement for it.

**Save-data continuity across app updates — direct requirement, added 2026-09-15, not yet designed**: whichever packaging path is used, updating the app (new .exe/.apk build) must NOT wipe or corrupt the player's existing scores/coins/achievements/etc. Right now everything lives in browser `localStorage` (see `saveAllTimeStats()`), which is fine for the current "open an HTML file" distribution but needs real thought once this becomes a packaged app: Tauri/Electron webviews and Capacitor's Android WebView each have their own persistence quirks across app-version upgrades (storage can be tied to a webview cache that an installer/updater might clear) — needs verifying per platform, not just assumed safe by default.
- [ ] **User ID / cross-device data portability — user's own idea, 2026-09-15**: give each player an ID at first launch; moving to a new device and entering that ID restores their save (overwriting whatever's on the new device). Explicitly floated as a *lightweight* fairness measure, not real anti-cheat — the user's own reasoning: someone could still hand a friend their save/ID, but at least it stops silently having two divergent saves under one identity, and lays groundwork for if/when any online leaderboard or server component gets built later (an ID to check submissions against). Needs real scoping before starting: how the ID is generated/stored, whether the restore is a manual paste-a-code flow or something fancier, whether "overwrite on restore" is really the wanted behavior (vs. e.g. picking the more-progressed save) — flagged as "maybe that's for the future," not asked for now.

---

## NEW IDEAS — 2026-09-15 dump, not yet triaged or scoped
A large batch from one voice-dictated message, explicitly "add it to plan / do things that are simple" — meaning most of this is intentionally NOT implemented yet, logged only. A few genuinely simple, unambiguous items from the same message were fixed immediately instead (see CHANGELOG Batch 395) and are not repeated here. Several items were phrased as direct questions to me ("what do you think?") — my actual answer to each is given inline, per house rule (answer the question, don't silently implement).

**Achievements**
- [x] **Already working, verified live (2026-09-15), no action needed** — user's idea: "achievements... that has like total number of something... after completion, it should show like the best one... even though it's bigger than the required." Checked `achModel()`: a completed tiered achievement already displays the real uncapped lifetime value against the ceiling (tested SCORE CHASER at 5,000,000 lifetime score against its 1,000,000 amethyst ceiling — displays "5M/1M", not capped at 1M).
- [ ] Idea: "some achievements also I think would be nice with displaying like mostly one times with progress bars still." Reading: give true ONE-TIME (non-tiered) achievements a progress bar too, not just a locked/unlocked pip. Needs real scoping — most one-time achievements (e.g. "jump clean over an ambulance") aren't backed by a numeric stat at all, so there's nothing to show partial progress toward; would need identifying which specific one-time achievements DO have an underlying countable stat worth visualizing.
- [x] **DONE (2026-09-21, Batch 444) — shipped as FULL WARDROBE (amethyst, one-time): every paint AND every booster. It was logged as blocked on the effects system existing, but it did not need to be - when effects land they get one more `every()` clause and the achievement widens by a line.** Original: New one-time achievement idea: own every cosmetic — all paint colors AND all boosters (and any future "effects", see below). Note: FULL PALETTE already exists as a tiered achievement for colors alone; this would be a broader "own every cosmetic" one-time achievement layered on top, not a replacement. Needs scoping once the effects system (below) exists, since "every cosmetic" isn't fully defined until effects are real.
- [x] **DONE (2026-09-21) — the per-tier reset stands; the user confirmed the decision is settled and does not want it revisited. tierReset: true is on 9 tiered achievements.** Original: **Tiered achievement tier-reset — reopened, my actual answer given in chat (2026-09-15)**: user asked whether the Batch 392 reset-per-tier decision is actually logical, "how do other games handle it," and to audit fairness by comparing same-tier-to-same-tier (amethyst vs amethyst, not amethyst vs diamond). See the chat response for the full answer — short version: most mainstream achievement systems (Steam/Xbox-style "kill 100/1,000/10,000 enemies") do NOT reset per tier, they track one lifetime number against each successive threshold; reset-to-zero-per-tier is closer to an RPG level-up mechanic. Not reverted — waiting on user's call after reading the comparison.

**Vanity / cosmetics (future)**
- [ ] "Effects" system — a new cosmetic category alongside paints/boosters: particle trail effects (smoke, rainbow, cute stars, "special tracks or something") attachable to any car — user's own framing: "effects will probably work with all cars" unlike the current boost-flame types, which are stock-car-only. Big feature, connects to the "cars effects... great for gifts" idea already logged further down this file and the vanity-collection achievement idea above. Not scoped.

**Visual / rendering (future, low priority — user's own framing)**
- [x] **DONE (2026-09-20, Batch 431)** — the two solid stripes per lane are removed; the lane boundaries, dashes and surface grain carry the road on their own.

**New obstacles (future)**
- [ ] Crosswalks — a hazard spanning the full road width, possibly with pedestrians on it, player must avoid. Big new feature: needs scoping (fail-on-hit vs. slow-down vs. purely visual timing hazard, spawn frequency, whether it interacts with existing traffic AI).
- [ ] Median/traffic-island obstacles — concrete islands between opposing lanes of travel, often carrying signage or traffic lights (red/orange/green), floated as possibly paired with crosswalks. Vague, needs real scoping — also worth checking first whether the game's current road model even represents "opposing direction" lanes at all, or if all lanes already run one direction (if the latter, this idea needs reframing before it can be scoped).

**Tutorial — DONE (2026-09-16, Batch 397)**
Built as `#tutorialView`, a normal `.view` (not a modal) with 4 tabs (BASICS/ADVANCED/MASTER/ABOUT) and 16 art+prose cards. Forced first launch works by ROUTING — opened instead of the menu when `allTimeStats.tutorialSeen` is false, so "nothing else clickable" is structural rather than an input trap; START DRIVING is the only way out and Escape is inert during that pass. Also reachable any time from the (previously disabled) TUTORIAL menu button. Design proposal in `docs/tutorial-design.md`, code-verified content facts in `docs/tutorial-facts.md`, UI convention map in `docs/tutorial-ui-map.md`. See CHANGELOG Batch 397.
- Deliberately NOT gated: all 4 tabs are freely browsable rather than requiring each section be completed — direct decision, forcing a reader through three tabs to reach the road fights the game. Reopen if the forced pass turns out to be too easy to skim past.
- [x] **Visual upgrade — DONE (2026-09-20, Batch 419)**: 6 animated road scenes, value chips, live "your setup" multiplier strip, first-paragraph takeaway emphasis, persisted read markers in the rail. See CHANGELOG Batch 419.
- [ ] Held from that batch: a "more" toggle collapsing each card's detail paragraphs (needs a card-by-card call on what stays visible). Not re-proposed: JUMP energy-bar mock (removed Batch 401) and START DRIVING (removed Batch 407).
- [ ] Still static, candidates if more animation is wanted: AMBULANCE TRADE, STAY CLOSE STAY PAID.
- [ ] **"Did you know?" launch tips — SPLIT OFF from the tutorial, not built.** The older 2026-09-06 onboarding entry (further down this file) also asked for a tip shown at game start, with a "don't show again" choice and a Settings toggle to disable tips generally. Deliberately scoped out of Batch 397 as its own system (tip pool + rotation + persistence + a settings row), not silently skipped. Needs: how many tips, whether they draw from the tutorial's own copy or are written separately, and whether "don't show again" is per-tip or global.

**Secret achievement hints — question, my answer given in chat (2026-09-15)**
User asked directly: is this a good idea, is it necessary, and where would hints even go? Not implemented — answer given in the chat response for this turn.

**Main menu redesign (future)**
- [x] **DONE (2026-09-20, Batch 426)** — the menu redesign gave every tab its own pixel icon and colour, and the hover frame now takes that colour too. Original note: Tab styling: the 8 secondary buttons below START RUN all look near-identical (plain rectangles); user wants icons and/or distinct colors/shapes per tab, floated a "puzzle piece" asymmetric-shape layout as one option. Also wants related tabs grouped closer together (Daily Gift/Achievements/Missions near each other; Stats/Scores/Garage near each other; Settings/Tutorial set apart). Needs a real visual design pass, not a code-first change.
- [x] **DONE (2026-09-20, Batches 426 + 432)** — lanes and difficulty live on the menu under START RUN, each showing its own multiplier, with the run total on START RUN itself; their Settings rows were removed in 432. Original note: Move LANES / DIFFICULTY (currently in Settings) into a larger box on the main menu directly under START RUN, showing the resulting score multiplier live as those are changed. Structurally distinct from the tab-styling idea above — could be scoped/built independently of the broader menu visual redesign.
- [ ] Booster preview popup: currently a plain unlabeled window with no consistent size across different boosters, which reads as "appears out of nowhere." Wants a consistent size and the booster's name shown in it.
- [x] **DONE (2026-09-21, Batch 444) — shipped as the secret ACT OF GOD. Cursor stays `auto` over the cars, as specified, so nothing advertises them as clickable.** Original: Easter egg + secret achievement idea: in the main menu's scrolling background gameplay preview, clicking a car (cursor must NOT change on hover — shouldn't tip off that it's clickable) triggers a destruction effect (explosion/rammed look, smoke) as a hidden Easter egg, paired with a new secret achievement for finding/doing it. Not scoped (click-target hit-testing against the animated preview scene, cooldown/repeatability, exact achievement condition).

**Tutorial (2026-09-20)**
- [x] **DONE (2026-09-20, Batch 424)** — all scenes play at 1.35x via a single TUT_SCENE_RATE. Original: *"in tutorial cars could go little faster"* — the Batch 419 animated scenes. Each scene sets its own NPC speed (roughly `0.04` px/ms, i.e. a car crosses the 88px scene in ~2s) and the road scrolls at `T * 0.05`. A straightforward tune: raise both together, or the traffic alone if the intent is "traffic moves faster past the player" rather than "everything is faster". Worth checking against the real game's feel rather than picking a number — the scenes are already slower than gameplay on purpose so the mechanic reads, so there is a limit past which they stop teaching.
- [x] **DONE (2026-09-20, Batch 424)** — first launch lands on the main menu; the TUTORIAL button blinks until opened once. Built as a NUDGE, not a gate (the open question below, resolved). Original: *"game starts in tutorial page (should start in main menu but highlight / force user to go to the tutorial"* — **a real reversal of the Batch 397 design decision**, which deliberately made the first launch open `#tutorialView` INSTEAD of the menu, by routing rather than by a modal, so nothing else was clickable. That is now unwanted: first launch should land on the main menu with the TUTORIAL button highlighted/attention-drawn, rather than opening the tutorial outright. Needs deciding: is it a *nudge* (menu loads, TUTORIAL button pulses/glows until visited) or still effectively *forced* (menu loads but only TUTORIAL is enabled)? Those are very different for a player who already knows the game. My take: nudge — a forced gate that the player can see around is more annoying than one they never reach, and the tutorial is freely reachable any time. The existing `allTimeStats.tutorialSeen` flag and the `forced` argument to `showTutorial()` are the hooks; the routing to change is the first-launch branch in the init block.

**Tutorial — content pass DONE (2026-09-20, Batch 425)**
- [x] STACKING card removed — it restated THE MULTIPLIER, whose live "your setup" strip now covers it better.
- [x] Read-dots in the contents rail removed, along with the whole tracking mechanism and the persisted `tutRead` field.
- [x] Both tutorial scrollbars (and the other four sharing the rule) made squarer again — pixel block with a hard 2px outline and inset bevel.
- [x] New **PROGRESSION** section, 5 cards: THE MAIN MENU, SPOTTING CARS, YOUR LEVEL, GIFTS/WORDS/MISSIONS, PERFORMANCE SCORE. Own purple, outside the difficulty ramp. All figures generated from the owning constants.
- [ ] Still possible additions to PROGRESSION if wanted later, deliberately not written yet: **ACHIEVEMENTS** (how tiers work, why some reset per tier and some don't — see the TIERED ACHIEVEMENT PROGRESS section below), **THE GARAGE** in its own right (paints, boosters, what BASE MULT on the hover card means), and **EXTRA BOXES** (currently only named in passing; they also need all 52 cars owned to appear, see the packaging/box notes above).
- [ ] MASTER is down to 3 cards now that STACKING is gone. Not a problem, but if a fourth is wanted it should be a genuinely master-level *driving* idea rather than another multiplier explanation.

**Achievements / icons (2026-09-20)**
- [ ] Original: *"better achv icons (look through older?)"* — the achievements currently share a **6-shape** icon set (`ACH_ICONS`: car, star, road, clock, bus, lock) across the whole roster, so most cards repeat one of six pictures; that repetition is very likely what reads as poor. Two parts: draw more shapes (one per achievement family at least), and "look through older" — the archived design docs are the place to check first, specifically `old/Achievements Tab.dc.html`, `old/achievementscontentbrief.html` and `old/Booster Icons 3x1.dc.html`, which may already contain an icon set that was never ported. Check those before drawing anything new. Same `[x,y,w,h,(inverted?)]` pixel-rect shorthand, 16x16 (the Batch 413 gear fix is the precedent for getting the viewBox right — it was blurring at 14 units drawn at 16px).
- [x] **DONE (Batch 427)** - replaced with the traffic-cone artwork supplied in `cone ico files/`; also embedded as the page favicon. `tools/make_icon.py` was repointed to `assets/car-icon.ico` so re-running it can never clobber supplied artwork. Original: *"app icon"* — the Windows app icon (`assets/recklessdriving.ico`, generated by `tools/make_icon.py` from the game's own `drawPixelCar()` on a strip of road). Currently a straight top-down car on asphalt, which is honest but plain at 16px where it is mostly seen. Worth a real pass: the car alone fills little of the square, and at small sizes the lane dashes turn to noise. Options: crop tighter to the car, drop the road, or use a recognisable silhouette instead. Regenerate with `python tools\make_icon.py` — keep it derived from the real sprite rather than hand-drawn.

**Result screen**
- [x] **DONE (2026-09-20, Batch 424)** — one shared treatment across all six panels, squarer thumb. Scrollbar styling.  Original note kept below. **Scrollbar styling (2026-09-20)**, original wording: *"in results screen when scroling achv (completed + progress) the scroll bar could look nicer (more egdy like squarely)"* — the achievements box on the result screen (`#goAchBox`). **Checked before logging**: it is not using the browser default — Batch-era CSS already themes it to match Garage/Stats/Daily Gift (`scrollbar-width: thin`, 10px, `--c-card-border` thumb on a `--c-panel` track with a 2px border, mint on hover). So the ask is to make that *shared* treatment squarer/more pixel-art, not to style one neglected scrollbar. Worth deciding up front: change it **everywhere** (`#goAchBox`, `.garage-scroll`, `.dg-scroll`, `.stats-scroll`, `.tut-scroll`, `.tut-rail` — six call sites with near-identical rules) so the UI stays consistent, or only on the result screen and accept the mismatch. My take: do it everywhere and factor the rules into one shared class rather than a sixth copy — the duplication is already the reason a tweak like this touches six places. Concretely "edgy/square" here means dropping `scrollbar-width: thin` (which rounds the thumb in Firefox), squaring the webkit thumb with no inset border, and possibly a 1px hard edge in `--c-panel-border` to match how `.btn`/`.tut-card` are drawn.

**Statistics screen**
- [x] **DONE (2026-09-16, Batch 414)** — renamed. Menu button and screen heading both now read STATISTICS/Statistics, matching the Scores screen's own jump-to button, which already used the long form. Internal ids/functions keep the `stats` prefix deliberately. Original note: user leans toward renaming "STATS" to "STATISTICS" to match the Scores screen's own wording, but phrased it as an open question ("do we stick with...?") rather than a firm decision, so held rather than renamed. My take: matching the Scores screen's own wording is a reasonable, low-risk consistency fix — worth doing whenever the visual pass below happens, or standalone if preferred.
- [ ] **Chart per-run dot markers — question, my answer**: user's concern — with few runs (or many very-similar-score runs), the line can render too smooth to see where individual data points actually are, even though hovering still works. My take: agree this is a real readability gap; adding a small dot/marker per actual data point (not just on hover) is a standard, low-risk chart-readability fix and wouldn't hurt the "smoother/more satisfying" look much at typical run counts — worth doing.
- [ ] **15 stats feels like a lot — question, my answer**: user's idea — keep the top 3 most important stats visible, move the rest behind either tab-switches (grouped by theme, e.g. score-related vs. time-related) or a single expand/show-more control. My take: the expand/show-more option is simpler to build and avoids inventing a grouping taxonomy that might not hold up (are all 15 stats cleanly groupable into 2-3 tabs, or would some awkwardly fit multiple groups?) — lean toward expand/collapse unless there's a natural, obvious 2-3-way split once actually looking at all 15 stat labels together.

**Daily Missions**
- [ ] **Progress bar style — question, my answer**: user dislikes the current 10-segment stepped bar (`.msn-seg`/`missionSegsHTML()`, own coin/XP color ramp) and wants the same continuous-width bar style already used for achievements (`.ach-fill`), open on whether to keep the mission-specific colors. My take: reusing the achievement bar's look is a reasonable consistency win and a fairly contained change (swap `missionSegsHTML()`'s stepped-pip markup for a single width% fill); keeping category-specific colors (coin vs. xp) seems fine to keep since it's still useful at-a-glance info, doesn't need to match achievements' tier-color convention.

**Daily Gift reward amount** — reopened from the previous turn's question (my answer given there: not obviously low, already scales with level, matches the ×50 economy rescale) — still waiting on what specifically feels low before touching numbers.

## NEW IDEAS — 2026-08-27 dump, not yet triaged or scoped
A batch of short notes from one message, explicitly "things for me to add (make future plans)" — not a request to implement any of this now, just to capture it. Every item below keeps the user's own original wording (verbatim, typos included) in a quote line, with my interpretation underneath it — in case my reading of a terse note turns out wrong, the raw source is right there to re-check against, not just my paraphrase of it. Most need a real scoping pass (and in a couple cases, a direct clarifying question) before starting. Grouped loosely by what kind of change each one is.

**Real bugs, worth checking first (cheapest to verify, most concrete)**
- [ ] Original: *"last cutscene wrong poliece positioning"*
  My reading: reported after Batch 98's lane-reuse fix (responders could drive through the wreck's own lane). Either that fix doesn't fully cover this case, or the user saw an older build before it landed — needs a fresh, specific look once actually played again before assuming the old fix is incomplete.
- [x] **Verified resolved (2026-09-08)** — Original: *"all car boxes in garadge should have the same size (first row doesnt for sure)"*
  Checked live across all 52 tiles in the current build (every rarity tier, every state — equipped/owned/not-owned/crate-locked/mystery "???"): all measure exactly 140px, no outliers. Whatever caused this earlier is already fixed (likely `.car-tile`'s `min-height:140px`, which now covers every state's actual content height) — nothing further to do here.

**Garage / economy**
- [ ] Original: *"cars effects (like fire, smoke, other?) great for gifts"*
  My reading: cosmetic particle-trail effects (fire, smoke, etc.) attachable to a car, framed as good reward content for the daily-gift system below, not necessarily coin-purchasable. Needs real scoping — which effects, how many, unlock method.

**Daily gift / colors (extends the existing entry further down this file)**
- [ ] Original: *"daily gift"*
  My reading: a general daily login-gift system (XP and/or coins) — see "GARAGE COLORS UNLOCKED VIA DAILY LETTER COLLECTION" further down, which already covers the letter-collection color-unlock angle; this note might mean the daily gift is meant to stand alone as its own system, not purely a vehicle for letter-collecting.
- [ ] Original: *"unlock colors through collecting letters (daily letters) or daily gift + more colors"*
  My reading: restates/reinforces the existing letter-collection entry below, plus a separate ask for more paint colors overall (beyond letter-collection unlocks specifically).

**UI/presentation passes needed**
- [x] **Done 2026-09-08, Batch 259**: the "animate level counting up" half — now animates after a run, a mission claim, or any Daily Gift/Word/Extra Box reward, on whichever of the 3 existing badge instances (Menu/Garage/Stats) is next shown. See CHANGELOG Batch 259.
- [ ] Still open: adding a 4th badge instance to the result screen itself (never had one at all) — original note floated it as an "or" alternative to animating the existing instances, not confirmed as still wanted on its own.

**Gameplay balance, unscoped**
- [ ] Original: *"more traffic on more lanes? multipliers"*
  My reading: more traffic density at higher lane counts, and/or reconsider how the lane-count multiplier bonus scales — phrased as a question by the user themselves (note the "?"), not a firm decision.

**Audio (2026-09-06)**
- [ ] Original: *"better explosion sound"*
  My reading: explicitly flagged for a future batch, not scoped further — the current Shield Bump/ram explosion sound (rebuilt in Batch 190, a lowpass-filtered low thud) isn't specified as broken, just due for improvement. Needs a real design pass (what should it sound like, does it vary by the 3 visual variants) before starting.

**Multi-lane balance (2026-09-06)**
- [ ] Original: *"maybe you should be the user should unlock multiple lanes, so like higher lanes by I don't know reaching some kind of level or like maybe even paying for it"*
  My reading: higher lane counts might become a level-gated or paid unlock, not available from the start, floated alongside the rare-car-spawn concern below (that part is now fixed — see CHANGELOG Batch 216). Needs real scoping — this specific idea hasn't been picked up.

**Traffic behavior bugs, reported not yet investigated (2026-09-06)** — verbatim wording preserved exactly
- [x] **Partially fixed 2026-09-08, Batch 264** — Original: *"I've noticed rare cases of cars going off top of each other, so like overlapping. I don't know if it's was like when switching lates but that that it just happened to look into it and maybe we should just in int int introduce some kind of logic that like when cars go either either one of them slows down and just doesn't crash or maybe they should crash and that will be like a event on the road and you should just watch out for the crash of cars."*
  Confirmed still happening, investigated via a real stress test (not guessing): reproduced and fixed one real cause — `canSpawnAt()`'s clearance check for a NEW vehicle spawning into a lane never accounted for how tall that new vehicle might turn out to be, only the existing one's height. Two tall trucks spawning back to back could end up overlapping, especially on suicidal's tight safety margin. Fixed, see CHANGELOG Batch 264.
- [x] **Fixed 2026-09-08, Batch 265** — the deeper issue found by the same stress test (two vehicles converging in the same lane over time via differing speeds, no lane-change involved). Direct decision: option (b), let them crash — see CHANGELOG Batch 265 for the `.wrecked` mechanic (shares ram-kill's wreck visuals, but stays a real collidable obstacle, unlike a ram-kill corpse).
- [x] **Verified resolved (2026-09-08)** — Original: *"the problem that is still in the game is that sometimes the cars when showing the switching lights still don't switch lines and I told you to make it so that if they show switching lines there should definitely switch lanes even if that means crashing into you"*
  Already fixed by an earlier batch (192, `laneClearForMerge`'s `checkPlayer=false` at commit-time) that predates this 2026-09-06 report reaching me — confirmed live: an NPC mid-lane-change with the PLAYER as its target lane, indicator about to expire, correctly commits into the player instead of aborting. NPC-vs-NPC overlap protection (a separate, legitimate concern) is untouched.

**Steering input bugs (2026-09-06)** — verbatim wording preserved exactly
- [x] **Actually resolved 2026-09-12, Batch 378** (Batch 377's default-flip was a real improvement but NOT the root cause — the user reported it still failed with the setting ON). Original: *"move and drag option doesnt work (car cannot change multiple lanes when holding the key)"*
  Real root cause, found only after asking the user what they actually SEE (not just "does it work"): the car visibly glides then stops after ~1 lane, still while the key is held. That's the signature of a genuine `keyup` firing mid-hold — some browser/OS/input-software combos report a real physical hold as rapid keyup+keydown pairs per repeat tick rather than one sustained keydown, which is exactly the one pattern none of the 3 prior investigation rounds (Batch 275, and this session's own 2 keyboard simulations) ever tested — all of them only simulated a single sustained keydown or repeated `repeat:true` keydowns with no keyup interspersed. Fixed with an 80ms release-debounce: `keyup` no longer clears the hold immediately, it schedules the release/snap 80ms later, and a same-direction `keydown` arriving first cancels it. See CHANGELOG Batch 378 for verification.

**UI/presentation (2026-09-06)** — verbatim wording preserved exactly
- [x] **Verified resolved (2026-09-08)** — Original: *"ram ability indicator in garage sometimes overlaps - i want to display it differently"*
  Checked live: all 9 `ramAbility` cars, forcing the "NEW!" tag active on every one simultaneously (rarity badge + NEW tag + ability badge all present at once — the worst case for this corner) — zero bounding-box overlaps found on any of them in the current build. Whatever caused this earlier is already fixed; nothing further to do.
**Daily Word (2026-09-06)** — verbatim wording preserved exactly
- [x] **Fixed 2026-09-08, Batch 257**: the "complete the same word over and over, more than one per day" bug report — confirmed still wanted despite an existing comment claiming it was intentional, capped to one completion per real day. See CHANGELOG Batch 257.
- [x] **Done 2026-09-08, Batch 258**: the "stays green" cosmetic idea — confirmed wanted, and confirmed to also mean removing `.dg-word-panel`'s border/background frame around it. See CHANGELOG Batch 258.

**Onboarding / help (2026-09-06)** — largely DELIVERED by Batch 397, see the Tutorial entry above
- [x] Parts (1) a HOW TO PLAY screen with instructions + illustrations, (2) skill-tiered sections, and (3) a mechanics-vs-tips split are all live as of Batch 397. Part (4), the start-of-game "did you know?" tips + Settings toggle, is still open — tracked as its own item under the Tutorial entry above.
- [x] **DONE (2026-09-16 onward, Batches 397-437)** — the TUTORIAL screen covers parts (1) instructions with illustrations, (2) skill tiers, (3) mechanics vs tips, and PROGRESSION was added in Batch 425. Only part (4), the "did you know?" launch tips, is still outstanding and is tracked on its own above. Original note: Original: *"new button- How to play seciotn - with instructions inside + images of things and how they work, then sections sorted into master level (for begginers, intermediete pros etc) tips and tricks and game mechanics + at the start of the game display 'did you know?' (you can select to not see it again - also disable/enable tips in settings)"*
  My reading: a substantial new feature, several distinct parts — (1) a new main-menu button opening a "HOW TO PLAY" screen with instructions and illustrations of game elements/mechanics; (2) that content organized into skill-level tiers (beginner/intermediate/pro); (3) a separate "tips and tricks" vs. "game mechanics" split within it; (4) a "did you know?" tip shown at game start, dismissible permanently via a "don't show again" choice, plus a Settings toggle to disable/enable tips generally. Needs real scoping before starting — likely several sub-batches (the screen itself, the content/copy for every tier, the start-of-game tip system, the settings toggle and its persistence), not one.

**Biomes — length/order/rarity DONE (2026-09-08, Batch 278)**
2x longer segments, order is now randomized per page load (was a fixed cycle) via weighted pick, real rarity tiers (common: meadow/forest/farm, semi-rare: suburb/city, rare: mountain/river/coast). See CHANGELOG Batch 278 for the exact weights and reasoning.

**New biomes + crossing variation — DONE (2026-09-08, Batch 279)**
6 new biomes added from the uploaded "Panorama Biomes v2.dc.html": Desert, Canyon, Industrial, Quarry, Marsh, Orchard — each with real ground art, prop scatter, and a rarity weight (my own call, see CHANGELOG Batch 279). River and side-road (underpass) crossings now roll a random angle/meander variant instead of always using the same fixed shape. Not covered from that doc: the doc's own alternate "existing biome" art style (a different, more advanced terrain-generation technique for meadow/coast/mountain/river) — not ported, since the game's existing versions of those 4 already work and porting would mean redoing already-working art for no requested reason.

**FPS counter — DONE (2026-09-08, Batch 268)**
Bottom-right FPS readout, Settings-toggleable (off by default, persisted). Kept small/faint grey per direct follow-up so it doesn't stand out.

**Pause screen button width — DONE (2026-09-08, Batch 253)**
`#pauseHudBtns` now caps at the same 340px as `.stat-box` above it, so RESUME/RESTART/QUIT never render wider than the info box on a wide (high-lane-count) canvas.

**Tank reload system — DONE (2026-09-08, Batches 272-273)**
Animation (Batch 272): on shooting, the ability bar drains fast to 0 over the shot's own duration, then recharges from 0 up to real energy over a fixed 1.5s regardless of the amount. Real lockout (Batch 273, direct follow-up confirmation): Tank now genuinely can't fire again until that full reload cycle finishes, closing the original "basically unkillable" complaint.

**Tank barrel/turret sprite gap — DONE (2026-09-08, Batch 270)**
Replaced with the "Tank Barrel Fix.dc.html" design doc's own "A · LONG GUN" variant — barrel drawn first (under the turret) instead of after it, hull front pulled back, mantlet bridges the seam. See CHANGELOG Batch 270. Same batch also locked Tank to one fixed color (direct follow-up, not originally part of this item) — see the now-closed multi-lane/color items below aren't affected, this is Tank-specific.

**Scores screen — DONE (2026-09-08, Batch 253)**
Added a STATISTICS button next to BACK in the Leaderboard/Scores screen, jumping straight to the Stats screen.

**Stats chart — DONE (2026-09-08, Batch 253)**
- The single yellow "best" dot is now a full dashed line across the chart at that value, with the actual number called out at the bottom-right below the chart.
- The floating hover tooltip (flagged repeatedly, going back to 2026-09-06 — see the "STATS / LEADERBOARD UX" section further down and the verbatim 2026-09-06 repeat above) is finally gone: hovering now repurposes the top-right AVERAGE readout to show the hovered run's key/date/value, reverting to AVERAGE when the hover ends.

**Speed realism question (2026-09-08)** — answered, no implementation needed
- [ ] Original: *"is the speed real? is speed correct with like car sizes?"* — follow-up clarifying the actual question: *"is the speed the car is moving proportional to its size (if the car this size went that fast would it in reality be X km/h?)"*
  Answer: No, there's no such relationship at all. A car's `h`/`hitboxW` (its sprite size) only drives its hitbox and the score-multiplier bonus (`vehicleMultiplierBonus` — longer cars are worth more points), completely disconnected from `CAR_KMH_OVERRIDE`, its speed rating — that's a hand-picked flavor number per car with zero size-based derivation. In real life bigger vehicles are usually slower, but nothing here enforces that either way — a long legendary car could just as easily be rated fast as a tiny one; it's whatever number was picked for flavor when that car was added, not a physics simulation.

**Car multiplier rebalance (2026-09-08)**
- [x] **DONE (2026-09-20, Batch 440)** — base 1.0, signed length term, curve 0.45. Range is now x0.83-x1.79 with Stock at exactly x1.00, top car just under lane x mode. Original: Original: *"lower cars mults - the lowest should be 1x or maybe the normal one (like gokart should have below 1x) - no need for like unnecesarry big mults, also look at them in comparison to other mults (if we nerf everythink check if like lanes or mode isnt worht way more than maybe taking the worst car (longest one) - tell me you balancing thoughts"*
  My reading: `carBaseMultiplier()` currently floors at 1.3x (every car, even Stock/Go-Kart) and scales up from there — wants the floor lowered so a small/nimble car like Go-Kart can sit BELOW 1x (an actual penalty, not just "less bonus"), while re-checking the whole multiplicative system (Batch 241's lane×mode×car) stays balanced relative to each other once car values shrink. Explicitly asked for balancing thoughts first — answered in chat, not implemented, needs the actual target numbers agreed before touching `carBaseMultiplier()`/`vehicleMultiplierBonus()`.

**Difficulty rename — DONE (2026-09-08, Batch 253)**
CALM → SAFE (button label + hidden `<select>` option text only; internal value stays `calm`, unaffected).

**Hours-played chart — DONE (2026-09-08, Batch 280)**
New "HOURS PLAYED" Stats chart mode, RUNS/% toggle. See CHANGELOG Batch 280 — one simplification from the original ask: shows run COUNT per hour (RUNS or %), not hours/minutes duration, since the game only tracks a start timestamp per run, not per-hour play duration.

**Booster types — DONE (2026-09-08, Batch 284)**
Generalized from stock-only to every jump-ability car (see CHANGELOG Batch 284 and the JUMP THRUSTER FLAMES section below, now also marked done). Also covers this file's separate NPC-color voice message: paints/boosters a car can't use are now hidden, not greyed out.

**Per-car booster availability list — DONE (2026-09-11, Batch 334)**
Full list given and implemented: `disabledFlames` overrides added to Land Speeder/Dragster/Jet Bike/Go-Kart/Tractor/F3 Junior/Superbike (Center Thruster only) and F1/Indy Oval/Formula (Rocket Pods off). School Bus/Fire Engine/Road Train/Garbage Truck/Limousine/Monster Truck/Tank/Steamroller were also named but already got the same Rocket-Pods-off behavior for free via the existing `ramAbility` default, confirmed car-by-car, no change needed.
Same batch also reworked booster UNLOCKING itself (a genuine partial reversal of Batch 289's "no gate at all"): Center Thruster is now the shared free default for both Jump and Ram, Underglow unlocks at player level 10, Rocket Pods costs 1,000,000 coins — see CHANGELOG Batch 334 for the full mechanism (`FLAME_UNLOCK_LEVEL`/`FLAME_BUY_COST`/`allTimeStats.ownedFlames`).

**Secret cars (2026-09-08)**
- [x] **DONE (2026-09-21, Batch 443) — the couch and the fish both exist now**, won from Extra Boxes rather than from secret missions. Original: Original: *"??? add secret cars (for completing secret missions?) fun ones: driving couch, fish etc"*
  My reading: novelty/joke car models (a couch, a fish, etc.) unlockable via secret achievements/missions once those exist — explicitly tentative ("???"), tied to the not-yet-built Achievements system below, not actionable on its own yet.

**Word truck — FIXED (2026-09-16, Batch 410)**
Root cause was never the art, which is why redrawing it repeatedly never worked: `drawWordTrackIcon()` deliberately `clearRect()`s its four corner pixels to round the silhouette, but `.dg-word-tiles-car` never opted out of the global `canvas { background-color: #5a5f66 }` rule — so those cleared pixels rendered as grey squares beside the headlights. One CSS declaration. Same root cause as the tutorial sprites (Batch 408).

**Word truck (2026-09-08) — original entry, now resolved**
- [x] **FIXED (2026-09-16, Batch 410)** — see the "Word truck — FIXED" entry just above; the cause was a CSS background showing through cleared corner pixels, not the art. This duplicate entry is closed. Original note: Original: *"fix the work truck"* — corrected by the user (2026-09-08) to *"word truck"*: this is `drawWordTrackIcon()`, the small truck icon used in the Daily Word progress track UI (Batch 175 already fixed one real bug there — a wrongly-transcribed rotation artifact — after 4 wrong attempts; see feature_daily_gift_word memory). Not yet clear whether this is a NEW complaint about the same icon or something else about it — what's actually wrong wasn't described this time either. Ask before starting.

**Missions dashed line — DONE (2026-09-08, Batch 253)**
Removed the dashed top border from the MISSIONS DONE / NEXT SET / CLEAN SWEEPS stat row specifically (kept on Daily Gift's own STREAK/BEST/CLAIMED row, which wasn't flagged and shares the same CSS class).

**ACHIEVEMENTS SYSTEM (2026-09-08)** — big dump, needs a real scoping pass before the actual roster is final. **Content brief delivered 2026-09-09** as an Artifact (organized one-time/tiered/secret, system behavior rules, no layout prescribed). **Screen/engine built 2026-09-10 (Batch 302)**, ported from the user's own "Achievements Tab.dc.html": `#missionsBtn` (ACHIEVEMENTS) is now live — full tabs (ALL/IN PROGRESS/COMPLETED) + sort (TYPE/PROGRESS/TIER) + tiered-ladder-expand + secret-veil UI, plus a result-screen "unlocked/progress this run" panel. Direct instruction for this pass: ship with a representative placeholder roster (9 achievements in `achData()`/`ACH_SECRETS`, carCrash.html), not the final list or thresholds — the real idea-list triage below is still the next step, not done by this pass. Of the 9 placeholders, 3 (TOTAL SCORE/PASS X CARS/SURVIVE X MINUTES) already read real lifetime allTimeStats fields; the rest (RAM X CARS, both ONE-TIME cards, all 3 SECRETs) have no dedicated tracking yet.
**Implementation in progress, 2026-09-10:**
- Batch 323 (phase 1): 8 real-stat TIERED achievements LIVE (Score Chaser, Overtaker, Staying Alive, Collector, Too Close To Comfort, Daily Grind, Climbing The Ranks, Task Master).
- Batch 324 (phase 2): 11 real ONE-TIME achievements LIVE, plus the new `allTimeStats.achievementsUnlocked`/`unlockAchievement()` persistence layer every future one-time/secret achievement will reuse (Clear The Way, Full Palette, Car Fanatic, I Am Speed, Adventuring Time, Truly A Reckless Driver, Not My Fault, High Roller, Early Bird, Night Owl, Two Of A Kind).
- Batch 325 (phase 2b): last 6 one-time achievements LIVE (Jackpot, Bad Samaritan, Spotter, Lucky Streak, Round The Clock, What A Monster). **The full 17-item ONE-TIME roster is done.**
- Batch 326 (phase 3): **14 of 15 SECRET achievements LIVE.** Mirror, Mirror needed genuinely new UI (click-to-spin on the level-badge ring, all 3 instances) — user then asked for a cooler interaction, see below.
- Batch 327: direct correction on "Pull Up! Pull Up!" — I'd wrongly called it impossible (confused "land ON TOP of a car" with "crash right after a mistimed jump landing"). Jump immunity ends the instant `abilityState` flips back to `'idle'` on landing, so a normal collision the very next frame was always possible — just needed a `lastJumpLandTs` timestamp + a "crashed within 500ms of landing" check. **Full 15/15 SECRET roster now real.**
- Batch 327 also fixed a real bug: the achievements header total excluded all SECRET achievements from the count (54 shown, should've been 69) — `achData().concat(ACH_SECRETS)` now used for the tally.
- Batch 328: trimmed "over your lifetime" from Overtaker/Too Close To Comfort/Task Master descriptions (redundant — tiered = cumulative by default), shortened Collector's description, added an ALL/TIERED/ONE-TIME/SECRET category filter row alongside the existing progress tabs.
- Batch 329: direct correction — Batch 328's sort picker AND category filter both REMOVED ("not really useful"). Replaced with fixed ordering: 3 sections always shown (TIERED/ONE-TIME/SECRET), always incomplete-before-complete. TIERED sorted by a hand-picked importance ranking (`TIERED_IMPORTANCE` const); ONE-TIME/SECRET sorted bronze→amethyst. Round The Clock/Lucky Streak descriptions shortened to one line.
- **Open design asks from the user, not yet implemented — need confirmation before building:**
  - Progress bars for some ONE-TIME achievements whose stat is genuinely countable (named examples: Round The Clock, Full Palette, Car Fanatic) + a separate UI section for these vs. plain single-unlock ones. Needs: which exact one-time achievements qualify (only the 3 named, or more?), and how the section split looks alongside the existing TYPE/PROGRESS/TIER sort — likely also needs the "TYPE" sort renamed to something that distinguishes progress-bar vs. plain-unlock, user asked for naming help.
  - Card box-sizing pass for the PROGRESS sort view — user flagged it "looks kind of weird" once cards of different bar-having-ness are mixed together, depends on the progress-bar decision above.
  - "Best" stat display (e.g. Spotter/Lucky Streak/I Am Speed showing best-ever even past the unlock threshold) — user asked for my opinion before deciding.
  - Small per-icon completion-COUNT badge for one-time/secret achievements — user is explicitly skeptical, asked for my honest take on whether it's worth it. Note: would require converting the `achievementsUnlocked` map from booleans to counters for every affected achievement, a real scope increase.
- Mirror, Mirror interaction redesign requested: instead of a CSS ring-rotate, reuse the existing ring-fill/XP-animation renderer — accelerating fake "spin" via repeated 0→100% fill laps, snap to a full ring at peak speed, hold briefly, ease back down to the real level %. Proposed to user, not yet built/confirmed.
- Batch 338 (2026-09-11): 2 direct bug reports fixed on the plain CSS-rotate version that actually shipped — the spin was replaying on every screen visit (leftover class + browser's display:none-restart behavior, now stripped via `animationend`), and the unlock had zero player-facing feedback (fixed with a one-time "ACHIEVEMENT UNLOCKED" confirm). The user then re-described a THIRD variant of the fancy animation idea — ring spins/fills but the true progress boundary stays visually fixed, settling back after one lap — explicitly said skip it if challenging. Judged genuinely nontrivial (new frame-loop canvas animation layer, real interference risk with the existing level-up fill system) and skipped this batch per that allowance; still open if wanted later.
- Batch 345 (2026-09-11): **all 51 car prices replaced with real-world-researched estimates**, direct request ("search for prices of cars and make them realistic"). `CAR_PRICE_SCALE` removed — prices are final absolute values now, not a ×50 base. Confirmed and flagged before implementing: real price doesn't respect rarity tier (a common Garbage Truck now costs more than several epics; a legendary Steamroller costs less than several commons) — accepted as real-world-accurate, not fixed by forcing numbers into bands. **User's own next step: re-tier some cars to match their real value** — not yet done, a separate follow-up whenever picked back up. See CHANGELOG Batch 345 for the full researched price list and which entries are judgment-call estimates (sci-fi/no-market-price vehicles).
- Batch 341 (2026-09-11): **MILLIONAIRE (one-time, current balance ≥10,000,000) is now real** — the "keep both" recommendation above was accepted; Businessman (lifetime-earned, already live since Batch 332) and Millionaire (at-once balance) now both exist as intended. Tier is AMETHYST per direct instruction (overriding this doc's earlier "I'd lean Diamond" guess) — also happens to fill the ONE-TIME roster's previously-empty amethyst slot. Same batch: Jackpot/Clear The Way/Not My Fault tier reassignments (bronze↔silver swaps), direct request.
- Batch 331 (phase 4): **Road Rage / High Flyer / Chain Reaction LIVE** — new `allTimeStats.totalRamKills`/`totalJumpClears`/`bestRamChainKills`, new `player.ramChainKills` (reset per ramming activation). Businessman still NOT added — waits on the economy-rescale go-ahead below. 43 total achievement cards now.
- Batch 332: **economy rescale done + Businessman added — the achievement roster is complete (44/44).** Direct instruction: "is there anything to do on the economy part excluding multiplying everything? if not then do it." Answer was yes (a lifetime-coins-earned stat, needed regardless of pricing) — both done together. See CHANGELOG Batch 332 for exact scale factors (prices/most earn-rate ×50, Missions ladder ×500) and the VEHICLE_POINTS-is-shared-with-score/XP care taken so only coins actually scaled. Number-display K/M abbreviation intentionally skipped that batch — not a real gap, `toLocaleString()` commas already read fine at 7 figures.
- Batch 333: 2 direct follow-up fixes. (1) Real bug: Batch 332's Missions ladder rescale hit XP missions too (shared ladder) — split into `MISSIONS_LADDER_COINS`/`MISSIONS_LADDER_XP`, XP reverted to its original numbers. (2) The K/M abbreviation from Batch 332's "skipped" note came back as a direct request after all — now live via `achFmt()` at the 3 coin-BALANCE displays (Garage/Daily Gift headers, Stats TOTAL COINS tile), each with the exact number on hover via `title`. Item price tags and the result screen's animated coin count-up deliberately left unabbreviated — see CHANGELOG Batch 333 for why.
**Full sort + numbers delivered 2026-09-10, revised 3x same day (rev.4)** as an Artifact ("Achievement Roster") — TIERED (10)/ONE-TIME (18)/SECRET (14), plus 1 still-parked idea ("score X in Xs", skipped for now) and 1 dropped entirely ("score X with the slowest car"). Rev 2 direct feedback applied: SURVIVE X MINUTES nerfed (2/5/10/15/30, kept as plain survival time not a composite metric — reasoning in the artifact); GET X CLOSE CALLS nerfed (25/100/250/500 — better players get fewer close calls, so a high ceiling becomes a wall as skill improves); OWN X CARS restructured (Diamond = all 39 buyable, Amethyst = all 52 incl. box-exclusives); HAVE X RUNS gained an Amethyst tier; several achievements renamed (Clear the Way, Jackpot, Car Fanatic, I Am Speed, Adventuring Time — a direct Minecraft nod); TRULY A RECKLESS DRIVER and SPOTTER (was Rare Sightings) moved tiered→one-time; EARLY BIRD/NIGHT OWL/NOT MY FAULT/LUCKY STREAK moved secret→one-time (luck isn't a repeatable trick, doesn't fit the secret rule); 2-ambulances-at-once confirmed genuinely achievable (user has hit it in-game) and added as a secret; lane-switch burst, 60-minute session, and 10x-multiplier ideas moved from parked into secrets with real numbers. Not yet implemented in code — this is still the numbers/sorting decision; wiring the real roster into `achData()`/`ACH_SECRETS` (carCrash.html) is the next step. The original idea list below is kept verbatim for reference:
> close call (be X pixels (or meters for better understanding)), ram the ambulance (possible?), get a close call with an ambulance, meet X rare/epic/legendary cars in one run (couple of achievements), die X times, score X total, complete daily gift/daily word/X missions (all missions?) in one run (tiered achievement), win a car from a box, get a paint 3 times in a row from boxes (whichever count) - how hard?, play early (early bird), play late, claim/complete missions/daily gift/daily word right before the reset, die within 5s, own X cars/all cars (tiered), score X (tiered), survive Xs (tiered), pass total X cars (tiered), jump over X times (tiered), ram X cars (tiered), shoot a car (secret achv or tiered?), jump over an ambulance X times (tiered? or maybe just 1 and make it secret), spot the monster truck ("what a monster"), something with different modes?, own all paints, do something with stock car, pass each biome, pass the same legendary car in one run X times, jump over X cars in one jump, ram X cars in one ram (tiered), have a run on each car, have X runs (tiered), play in every hour of the day, reach a streak of X (tiered), play for X minutes straight (name: "Addicted?"), get run over by ambulance at top speed ("Caught Up"?), get X close calls (tiered), get 2 close calls in a row (two passed cars back to back), get X close calls in one run (tiered) ("Truly a Reckless Driver"?), change X lanes in X seconds, get X score in Xs (e.g. 2000 in 5s, hard, close-call-driven), crash on a legendary car ("Eye Catching"? or better), drive at 250km/h, have the highest possible mult in actual gameplay, crash into the school bus ("No School Today"), have a close call at <60km/h ("Bad Driver"), crash on a road obstacle once those exist ("You Can't Park There, Sir") + other obstacle achievements, use a legendary car with a non-default paint AND booster ("Dripped Out"), get X score with the slowest car, land on top of a car and crash ("Pull Up! Pull Up!").
>
> System-level design notes from the same message: some achievements one-time, some tiered; unlocking a new one shows on the result screen; sections split into {tiered (sub-sectioned if too many), one-time, secret}; some fun-but-not-too-hard secret ones; tiered achievements show the highest COMPLETED tier under "completed" and the next INCOMPLETE tier under "in progress" (lower tiers must be done before a higher one counts); grouped into tabs {ALL, COMPLETED, UNCOMPLETED}; open invitation to rename any of the suggested names if a better one exists, and to add more ideas.

**New batch of ideas (2026-09-10), rev.2 after direct follow-up feedback** — not yet in the Artifact, roster/artifact update is the next step. Discarded ideas removed outright per direct instruction rather than kept crossed-out.
- [x] **BUILT as NOTHING LEFT HIDDEN (2026-09-20, Batch 438)** — **"Nothing Left Hidden"** — ONE-TIME, Amethyst. Unlock every secret achievement. ("Detective" considered as an alt name, direct call was to keep this one.)
- [ ] **Big endgame reward — special car with manual speed control.** Real feature, not just achievement numbers; unlocked after completing "everything" (exact end-state definition still TBD). Direct follow-up clarifies the multiplier question: **the live speed-based multiplier bonus already exists** (`calculateMultiplier()`'s live term — roughly +1% of base multiplier per km/h above 50, see carCrash.html ~13496-13505) — so a manual-speed car slots into the EXISTING formula with zero multiplier-system changes; drive faster manually, multiplier responds exactly like it already does for every other car. The other half of the idea — the ram ability itself directly boosting multiplier, not just via speed — is NOT currently a thing and would be a genuinely new addition if still wanted; flagging as a separate open question, not assumed. Still needs its own scoping pass for the rest (end-state definition, UI for the new speed control) before buildable.
- [x] **ALREADY EXISTED as CLIMBING THE RANKS (found while building Batch 438)** — **Player level, TIERED — needs a fan name + description**, direct request. Working title **"Climbing the Ranks"**, description "Reach a certain player level." Real stat (`allTimeStats.playerLevel`), cap `REWARD_LEVEL_CAP = 100`. Bronze 10 / Silver 25 / Gold 50 / Diamond 75 / Amethyst 100.
- [x] **BUILT as PERFECT DAY (2026-09-20, Batch 438)** — **"Perfect Day"** — ONE-TIME, Gold. Claim the Daily Gift, complete the Daily Word, AND clean-sweep all 6 Daily Missions, same calendar day.
- [x] **ALREADY EXISTED as TASK MASTER (found while building Batch 438)** — **Missions completed, TIERED** — real stat (`allTimeStats.missionsDoneTotal`, max 6/day). Direct numbers: Bronze 10 / Silver 20 / Gold 50 / Diamond 100 (caps at Diamond, no Amethyst). **Daily Gifts claimed and Daily Words completed tiered ideas explicitly dropped** — direct call not to make an achievement out of every single system, Missions alone is enough here.
- [x] **BUILT as LANE HOPPER (2026-09-20, Batch 438)** — **Lane switches, lifetime, TIERED** — real stat (`allTimeStats.totalLaneSwitches`), confirmed good idea. Working title **"Lane Hopper"** (kept distinct from the "Serpentine" secret below, same underlying action but a different framing — lifetime total vs. a fast burst). Bronze 100 / Silver 500 / Gold 2,000 / Diamond 10,000.
- [x] **BUILT as WORDSMITH (2026-09-20, Batch 438)** — **"Wordsmith" — redefined per direct follow-up.** Not the crash-stamp words (that idea didn't land) — collect/complete every one of the 39 `DAILY_WORDS` at least once. ONE-TIME with a progress bar (the existing SECRET `goal`-type pattern, e.g. `v`/`goal`, no tier ladder), Diamond. Description: "Complete every one of the 39 Daily Words at least once." **Best-coins-in-one-run idea dropped** — direct call, not needed.
- [x] **BUILT as SOUND OF SILENCE (2026-09-20, Batch 438)** — **"Sound of Silence" — corrected per direct follow-up, real UX point.** Basing it on the 4 category sliders (not Master) made it something no real player would ever naturally do — anyone wanting silence just uses Master, so a "zero out 4 separate sliders while leaving Master alone" combo is effectively undiscoverable, a bad secret. Now: SECRET, Bronze, triggers on **Master Volume set to 0**. **"Turn It Up To Eleven" dropped entirely** — direct call.
- [x] **DONE (2026-09-21) — the coin economy was rescaled and MILLIONAIRE shipped as a tiered achievement on totalCoinsEarnedLifetime.** Original: **"Millionaire" / coin economy — updated opinion after a real scope check**, direct follow-up. Surveyed the actual coin economy (recorded 2026-09-10): per-run earning is 2 formula divisors + the shared `VEHICLE_POINTS` table (~10-15 entries); Extra Box is 1 base constant; Missions is 3 numbers (`MISSIONS_LADDER`); Daily Gift/Word coin rewards are ~8 shared formula constants, not per-item; paint costs are 3 nonzero numbers. The one genuinely large piece is **51 individual hardcoded car `unlock` prices** (currently 60-20,000, tiers already overlap rather than sitting in clean bands). **Revised take: this is smaller than I first estimated, and the user's own assessment was right** — it's a real, contained rebalance (mainly the car price column + raising the 2 earn-rate divisors together so pacing stays roughly intact), not a sprawling project. Still recommend layering in a separate `allTimeStats.totalCoinsEarnedLifetime` field (balance goes DOWN when spent, so Millionaire needs a lifetime-earned counter regardless of pricing) — **MILLIONAIRE as a TIERED achievement** on that stat: Bronze 1,000 / Silver 10,000 / Gold 100,000 / Diamond 500,000 / Amethyst 1,000,000 (name covers the whole ladder). Motivation is confirmed broader than just fitting one achievement: real "dollar" flavor, room for a bigger tier of achievement (a "Multi-Millionaire" ceiling was floated), and — the strongest practical argument — more headroom to fine-tune individual reward values relative to each other than today's single-digit rewards allow. **Not yet implemented** — this is opinion/scoping only, waiting on a go-ahead before touching any prices.
- [x] **DONE (2026-09-21) — superseded by the rescale actually landing - the price spread quoted here (60 to 20,000) is long gone; prices now run up to 18,500,000.** Original: **Real numbers pulled per direct request (2026-09-10), correcting my own earlier claim** — the user was right that price change DOES make Millionaire work, because the actual lever is earn RATE, not price alone; my "add a lifetime counter" fix doesn't help if the underlying earn rate stays tiny (1M lifetime earned would take absurdly long at today's rate). Verified current numbers directly in code: cheapest car **60** (Hot Hatch, common), most expensive **20,000** (Steamroller, legendary) — a **~333x** spread; by tier, common 60-320, rare 650-2,100, epic 2,800-7,400, legendary 8,500-20,000 (bands already overlap today, nothing clean to preserve). Typical earn rate, computed from the real formulas (`VEHICLE_POINTS` 10-50, `coinsThisRun += round(pts/10)` per pass, `finalCoins = round(coinsThisRun * multiplier)`, ~38-42 credited passes/min at reckless/4-lane, ~2.65x typical crash-time multiplier): **~175 coins per typical (~90s) run, ~6,000 coins/hour** of continuous play. At that rate the current most expensive car (20,000) already takes ~115 runs / **~3.3 hours** to save up outright.
  Target check: user's stated targets (cheapest "a couple thousand", most expensive "1-2 million") land almost exactly on a **flat ~50x multiplier applied to every existing price** — 60→3,000, 20,000→1,000,000 — no curve-reshaping needed, a uniform scale hits both ends near enough on its own.
  **The earn side has to scale by the same ~50x too**, or the grind gets 50x harder for no reason. Do that (⇒ ~8,750 coins/run, ~300,000 coins/hour) and the relative pacing per car is IDENTICAL to today — nothing about actual difficulty changes, only the numbers get bigger/more "dollar-like." Under that scaled rate, 1,000,000 lifetime-earned (Millionaire's peak) costs about the same real playtime as saving for today's single most expensive car already does: **~3.3 hours**. That reads as a genuine, substantial top-tier goal — I'd lean Diamond rather than Gold for that specific number, though Gold is defensible if the intent is for it to be more casually reachable. Re-scaled the whole `MILLIONAIRE` ladder to match (the earlier 1K-1M draft was calibrated to the OLD tiny economy and would now be nearly instant at every tier): **Bronze 10,000 (~7 min) / Silver 100,000 (~20 min) / Gold 400,000 (~1.3 hr) / Diamond 1,000,000 (~3.3 hr)**.
  On the "common cars become too easy" worry: under a uniform scale, they're **no easier relative to earn rate than today** — a Hot Hatch already costs under one run's earnings right now, so that's not a new problem the rescale introduces, it's already how the game behaves. Making commons feel like a deliberate purchase (not instant) would need prices to scale MORE than earn-rate specifically at the cheap end — a real, separate curve-shaping choice, not something the realism goal itself requires. Flagging as optional, not assumed.
  Still not implemented — numbers only, waiting for a go-ahead on the actual scale-up (and confirmation on the commons-should-cost-more-relative-to-earn-rate question) before touching code.
- [x] **DONE (2026-09-21) — built. Car prices and per-pass earn rates were rescaled together as specced.** Original: **Economy rework — final direction settled (2026-09-10)**, direct decision: **yes, do it.** No real gameplay downside if earn rate and prices scale together (preserves today's pacing, just bigger/more "dollar-like" numbers), clear upsides (achievement fit, more reward-tuning headroom, better feel). Two things confirmed ALREADY TRUE in the current code, no new work needed for either: (1) `finalCoins = round(coinsThisRun * multiplier)` — coin payout is already multiplier-scaled, so a beginner's shorter/slower run already earns less than a skilled player's faster/longer one, the "learning curve" effect already exists; (2) `DIFFICULTY_MULT_BONUS` already gives Suicidal a bigger multiplier bonus than Reckless/Calm (+0.4 vs +0.15 vs +0), so higher difficulty already earns proportionally faster today — this carries over unchanged under a rescale since it's a relative multiplier, not an absolute number.
  **Full current coin-source list, as requested:**
  - Grounded pass (not jumped): `+round(pts/10)` coins, where `pts` = `VEHICLE_POINTS[type]` (normal/motorbike/tractor=10, reckless/truck=20, ambulance=50) → **+1** normal/motorbike/tractor, **+2** reckless/truck, **+5** ambulance.
  - Jump-over pass: `+round(pts/2)` instead (5x the grounded rate, per point) → **+5** normal, **+10** reckless/truck, **+25** ambulance.
  - Run payout: `finalCoins = round(coinsThisRun * multiplier)` — the above raw totals get scaled by the run's live multiplier at crash time (typically ~1.7-2.7x on Reckless).
  - Daily Missions: `MISSIONS_LADDER = [100, 200, 400]` — 3 coin-category mission slots, escalating by claim order.
  - Daily Gift (coin-tier roll): `lower = 100 + 5*(level-1)`, `upper = lower*1.5`.
  - Daily Word (coin-tier roll): same formula, ×2 (`REWARD_WORD_MULT`).
  - Extra Box duplicate-car compensation (when a box gives a car you already own): `CAR_DUPLICATE_BASE = {common:1000, rare:2500, epic:5000, legendary:10000}`.
  - Dev menu "Add Coins": manual testing tool, default 1,000, not part of the real economy.
  **Achievement naming/structure, direct decision**: rename the tiered ladder from "Millionaire" to **"Businessman"** (Bronze 10k doesn't read as a millionaire) — keep the same lifetime-earned tiers (10k/100k/400k/1M). Floated scrapping it for a single "Millionaire" one-time (current BALANCE ≥ 10,000,000 at once, not lifetime-earned) instead, or keeping both. **My recommendation: keep both** — they're genuinely different challenges: Businessman rewards steady lifetime play regardless of spending habits, while a separate one-time **MILLIONAIRE** (Diamond, current balance ≥ 10,000,000 at once) is a deliberate hoarding/restraint challenge — don't spend for a long stretch, a distinct kind of hard. At the rescaled ~300k/hr earn rate that's ~33 hours of pure saving, a fair late-game Diamond goal.
  **Number display for big coin values — DONE (2026-09-11, Batch 335, final form after 2 correction rounds).** Ended up with the toggle option after all (Batch 335 direct request), not the fixed-rule recommendation above: new `coinAbbr()`/`coinDisplay()` (separate from `achFmt()`, which stays achievement-only) abbreviate starting at 1,000 with 1 decimal on both K and M tiers (5,300→"5.3K"), gated by a new Settings toggle (ABBREVIATE MONEY, default ON, off = always exact everywhere). Applied to Garage/Daily Gift coin totals (hover swaps the displayed text itself to the exact number, no tooltip) and Garage car-tile price badges. Stats' TOTAL COINS deliberately stays plain always (direct correction, Batch 335) — NOT abbreviated, unlike the recommendation above assumed. Buy-confirm dialogs and the "LOCKED — X COINS" description text also stay exact always, regardless of the toggle.
  Still not implemented — this is the full, now-settled direction; actual code changes (price rescale, earn-rate rescale, the two achievements, number formatting) still need a final go-ahead before anything is touched.
- [x] **DONE (2026-09-21) — built exactly as proposed - MISSIONS_LADDER_COINS is [50000, 100000, 200000], the ~500x fast track, against the ~50x used for prices.** Original: **Pacing recalibrated (2026-09-10), direct pushback**: "~33 hours for the top car" (my earlier "preserve today's exact pacing, just bigger numbers" proposal) is way too slow for a small/simple game — even an hour for a legendary car already reads as a lot. **Resolution: don't fix this by inflating the base per-pass earn rate to match.** Doing that would make ordinary grinding trivial and cheapen the core loop. Instead, lean on the **Daily Missions coin ladder as the deliberate fast track**: direct proposal is `MISSIONS_LADDER` scaling ~500x instead of the ~50x used for prices — **[100,200,400] → [50,000, 100,000, 200,000]**, all 3 coin missions in one day ≈ 350,000 coins. That's real, coherent design: missions are day-limited (can't be repeated/grinded), so making them disproportionately generous is what makes them feel worth doing and pulls players back daily — normal grinding stays the slower fallback, not the intended route to big purchases. A few days of daily missions gets even the top car, addressing the "an hour is already a lot" complaint without gutting the baseline economy. Base per-pass rates still scale with the general ~50x price rescale (unchanged from the earlier plan) — only the missions ladder gets the extra boost.
  On multiplier "also increasing coins" — **already true** for run earnings (`finalCoins = coinsThisRun * multiplier`, confirmed earlier), no change needed there; this does NOT apply to Missions, which pay a flat claimed amount unrelated to any one run's multiplier — kept that way deliberately, a flat guaranteed "paycheck" is easier to plan around than a run-dependent one.
  **Should missions scale with level at all? — real open question raised, here's my take:** mission REWARDS don't scale with level (the ladder is a flat 50k/100k/200k for everyone) — so there's no strong case for making the GOAL scale up much either, since a higher-level player isn't being paid more for the harder ask. Recommend keeping goal-scaling **gentle across the whole pool**, not aggressive — Overtake's now-raised 2/level (per direct request, implemented — Batch 322) is on the more aggressive end and worth watching; Ram's existing +1/5-levels and Witness/Biomes' flat-no-scaling are closer to the shape I'd suggest for the rest of the pool too. Not touching the others yet, flagging for a look together.
- [x] **DONE (2026-09-21, Batch 444) — shipped as the secret FULL CIRCLE. The ring sweeps from its real fill all the way round and lands back on it; `(real + eased) % 1` guarantees it settles exactly where it started. Works on all 3 badges - the menu's uses .mm-level-* markup, Garage and Stats use .level-badge-*, and the handler matches both.** Original: **New secret achievement idea (2026-09-08)**, original wording: *"click on the players level : animation : progress bar goes like up (does whole circle and comes to its place)"* — clicking the level badge (any of the 3 instances) triggers a fun bonus animation: the ring spins a full extra loop around before settling back into its real current fill position. Ties into the counting-up animation infrastructure just built (Batch 259) — could reuse `paintLevelBadgeAt()` per-frame the same way.
- [x] **ALREADY EXISTED as NOT MY FAULT (found while building Batch 438)** — **New achievement idea (2026-09-08)**, original wording: *"spot a car crash"* — ties directly into the new NPC-vs-NPC crash mechanic (Batch 265-267): an achievement for witnessing two other cars crash into each other on the road, separate from the player's own ram/jump/pass achievements.
- [x] **ALREADY EXISTED as CEASE AND DESIST (found while building Batch 438)** — **New achievement idea (2026-09-08)**, original wording: *"shoot the ambulance"* — Tank-specific (shooting is Tank's own ram-equivalent), distinct from the general "ram the ambulance" idea already listed above since it's tied to the shot mechanic specifically, not physical ramming.
- [x] **DONE (2026-09-21) — shipped as ADVENTURING TIME, a one-time gold achievement reading BIOME_DEFS.length, so the goal number follows the biome list automatically.** Original: **New achievement idea (2026-09-08)**, original wording: *"be in all biomes in one run"* — pass through every named biome type at least once within a single run. Now that biome order is randomized per session (Batch 278) rather than a fixed cycle, this is a genuine run-length/luck-dependent challenge, not just "wait long enough" — worth noting when scoping the actual goal number (currently 14 biome types, see BIOME_DEFS).
- [x] **BUILT as DOUBLE SHIFT (2026-09-20, Batch 438)** — **New achievement idea (2026-09-08)**, original wording: *"see 2 ambulances at the same time (possible)"* — flagged by the user themselves as maybe not achievable ("possible"?): needs a check of whether the game's spawn logic can ever actually have 2 ambulances on screen simultaneously before treating this as real design work, not just an achievement-list entry.
- [x] **ALREADY EXISTED as CHAIN REACTION (found while building Batch 438)** — **New achievement idea (2026-09-09)**, original wording: *"ram 20 wechicles in one ramming"* — ram 20 vehicles within a single Shield Bump ramming window (one continuous `abilityState: 'ramming'` activation), not 20 over a whole run. Tiered-achievement candidate like the other "X in one Y" ideas above.
- [x] **BUILT as NOTHING TO SEE HERE (2026-09-20, Batch 438)** — **New achievement + mission idea (2026-09-20)**, original wording: *"achv/mission: jump over road obstacle"* — clear a breakdown obstacle (Batch 288) by jumping it. **Checked how jumps are counted before logging this**: the jump-clear counter fires in the main collision loop on *any* `Vehicle` the player passes through while airborne, and a breakdown is a `Vehicle` with `type === 'obstacle'` — so jumping one already counts toward `jumpedOverCount`, `allTimeStats.totalJumpClears` and the `jump_over_cars` mission, indistinguishably from jumping an ordinary car. Making this its own achievement/mission therefore needs a *separate* counter, not a new use of the existing one; the cheap version is a per-run `obstaclesJumpedThisRun` incremented in that same branch when `v.type === 'obstacle'`, plus a lifetime total for the tiered version. Worth deciding at the same time: one-time ("jump a breakdown at all", easy and a nice surprise) vs tiered (needs real numbers — obstacles are rare per run, so a high ceiling would be a long grind). Pairs with the existing `jump_ambulance` mission, which is the same shape.
- [x] **BUILT as ROAD TRIP (2026-09-20, Batch 438)** — **New achievement idea (2026-09-20)**, original wording: *"achv: all time km rode"* — a TIERED achievement on total kilometres driven over the player's lifetime. **Checked the code before logging this: there is no distance tracking of any kind today** — no per-run distance, no lifetime one. `allTimeStats` has `totalGameTimeMs` (time) but nothing in distance, and `roadOffset` is a wrapping scroll position (`ROAD_OFFSET_WRAP`), not an odometer, so it can't be read as one retroactively. So this needs a genuinely new stat: integrate `speedToKmh(currentSpeed)` over real elapsed time each frame into a per-run distance, then add it into a new `allTimeStats.totalDistanceKm` at run end. Two things worth settling before building: (1) km here is a flavour unit — it comes out of `speedToKmh()`, the same display conversion the HUD uses, so "km driven" is at least internally consistent with the speed the player sees, but it is not a physical simulation (see the already-answered "is the speed real?" entry above); (2) tier numbers should be picked from a real measurement of a typical run's distance, not guessed — the same mistake the Millionaire ladder made when it was calibrated against the wrong economy. Pairs naturally with the existing time-based STAYING ALIVE ladder (time survived) as its distance counterpart.
- [x] **ALREADY EXISTED as DIDN'T EVEN TRY (found while building Batch 438)** — **New achievement idea (2026-09-09)**, original wording: *"have a die with zero score, and something like a stupid die"* — one-time achievement for ending a run with exactly 0 points, distinct from the existing "die within 5 seconds" idea (that one's about TIME, this is about SCORE — a slow 0-point death, e.g. sitting still or reversing the whole run, would also qualify). Working title suggestion: "Didn't Even Try" — open to a better name.

---

## TECHNICAL (mechanical/gameplay impact — do these first)

## BATCH 49 — Static Road Obstacles — DONE (2026-09-08, Batch 288)
Implemented as the "breakdown" obstacle from "Road Decals + Obstacles.dc.html": a real parked NPC car (random body/paint from the existing pool) + barrier/cones + a 17x15 flashing warning sign during a pre-warning countdown, same spawn pipeline as traffic, ends the run on contact like any vehicle. Rockfall/cow/deer variants from the same doc explicitly dropped ("looks pretty weird"). See CHANGELOG Batch 288. Obstacle spawn frequency was a judgment call (doc left it open) — revisit if it feels too common/rare.
- [ ] Road-surface decals (oil/scuffed dash/manholes, Batch 287) also came from this same doc — the doc's fuller decal set (patches/potholes/drains/skid marks) was NOT implemented, only what was explicitly asked for.
- [ ] **Minor structural note (2026-09-08, from Batch 288's review pass)**: `canSpawnAt()`'s "at least one lane stays free" guarantee only checks already-spawned traffic (`topBlockedLanes`), not lanes currently mid-countdown in `pendingAmbulances`/`pendingObstacles` — in principle two independent low-probability pre-warnings could land in different lanes in the same window and transiently over-reserve, worst-case at low lane counts. Pre-existing pattern (the ambulance system already worked this way), estimated well under 1% per run at current rates — not fixed, just flagged in case obstacle frequency ever gets tuned up or a third reserving hazard type is added later.

## BATCH 89 FOLLOW-UP — vehicle mechanics not yet implemented
- [ ] Still not implemented: the doc's own per-vehicle SPEED multiplier (`0.5×` to `1.6×`, still display-only/pricing-only, no gameplay effect — superseded for actual gameplay by Batch 167's real per-vehicle speed cap, see below), and every Oddity-class gimmick described in the doc's notes (Armored Van eats one hit per run, Monster Truck rolls over a hatchback twice per run, Tank crushes on touch but turns slowly, Ice Cream Van spawns coin pickups in pairs, Steamroller flattens traffic, etc.) — these are flavor-text ideas from the design doc, not yet scoped as real mechanics.
- [ ] **New, direct elaboration (2026-08-27)**: this won't be "every car gets its own unique ability" — instead, some cars share a small set of distinct ability TYPES (now real, see CHANGELOG Batch 193+ for Shield Bump/Tank). Still open: whether any FURTHER distinct ability types beyond ram/shoot get added — "a couple more" was floated but nothing further has been named.
- [ ] **Energy bar capacity (2026-09-05), original wording, still unresolved**: "full energy bar allows for 50% more jumping." My reading, not yet confirmed: the bar's total capacity should be sized so that spending a full bar entirely on Jump (never on Shield Bump) yields 50% MORE total jump uses/duration than whatever the bar allows today — i.e. the bar itself is growing, not just being reallocated. Not touched yet (Jump's own constants — `JUMP_FULL_DRAIN_MS` etc. — are untouched). Flag if this reading is wrong.
- [ ] **Still open**: the user's own "+1% multiplier per km/h gained" formula was not implemented for the real per-vehicle speed system (Batch 167) — the existing proportional multiplier formula was kept as-is, just re-anchored to a shared reference ceiling. Revisit if the user specifically wants that exact linear formula: +1% multiplier per km/h gained from start to that car's own top speed (e.g. 50→150 = 100 km/h gained = +100%/×2 total; 50→250 = 200 km/h gained = +200%/×3 total). One thing to watch if picked up: the formula is linear with no ceiling, so it keeps scaling forever if an even-faster car is added far in the future. Every Oddity-class gimmick (Armored Van eats a hit, Tank crushes on touch, etc.) also remains unimplemented, unrelated to this speed work.

## COLOR ECONOMY
- [ ] The user's own concrete open question, not yet revisited: whether an even-further color-tier expansion is wanted beyond the current 3-tier (free/buy/gift) + Prism system. No specific ask yet — flag only if raised again.

## GARAGE CARS AS NPC TRAFFIC
- [ ] Separately noted in passing, not a request to fix: Limousine's sprite reads as similar to a bus at this pixel scale (long/thin body, repeated side windows) — both are native long-thin shapes in this art style, so some visual overlap between "long car" silhouettes is somewhat inherent; flag if this specific pair still bothers the user once actually seen.

## DAILY GIFT / DAILY WORD SYSTEM
- [x] **DONE (2026-09-10, Batch 302)** — the `#missionsBtn` placeholder (Batch 122, sits next to Daily Gift on the main menu, labeled ACHIEVEMENTS since Batch 180) is now wired up: see the ACHIEVEMENTS SYSTEM entry further up this file for what shipped and what's still placeholder.
- [ ] **Still simplified from the design doc, not yet revisited**: the doc's "unclaimed rewards are lost at midnight" copy was dropped since rewards actually grant atomically in this implementation, there's no real at-risk window to warn about.
- [x] **Confirmed resolved (2026-09-09, direct confirmation)** — the 2026-09-06 backlog entry (NEW IDEAS section) reporting the same day's word could be completed/claimed more than once. Already fixed by Batch 257's once-per-day cap; user confirmed it's resolved.

## DAILY MISSIONS
- [ ] Not yet built: any UI change to reflect a mission's rolled `extra` params more visually (e.g. the difficulty mission shows the assigned difficulty only in its name/hint text, no distinct badge/icon) — cosmetic-only, not blocking.
- [x] **DONE (2026-09-10, Batch 320)** — new mission ideas, checked against the real current `MISSION_DEFS` pool first. Direct follow-up: dropped `reach_multiplier` entirely instead of tweaking it (too swingy — trivially cheesed with the right car/lane/difficulty combo, or unreasonably hard otherwise) and shipped the 4 genuinely-missing ones below with real tracking, not just numbers.
  - **Already exist, no action needed**: "meet 5 ambulances" = `meet_ambulances` (MEET X AMBULANCES); "reach a big multiplier" = `reach_multiplier` (REACH A Gx MULTIPLIER); "total score this day" = `score_overall` (SCORE X TOTAL TODAY); "jump over X cars" = `jump_over_cars`.
  - **One easy tweak, not a new mission**: `reach_multiplier`'s goal is currently a flat, unscaled **3.0** every time — direct ask was closer to 4-5x ("pretty big but not super big"). Suggest replacing the flat value with a small pool like `missionPickOne([3.5, 4.0, 4.5, 5.0])`, matching the exact pattern `reach_speed` already uses for its own goal variety.
  - **Genuinely missing, worth adding** (none of the 14 current missions cover these):
    - **Overtake/pass X cars** — surprisingly absent given how central passing is; trivial to add since `dodgedCount`/`creditVehiclePass()` already tracks every pass live. Suggested goal: `missionRoundTo(50 + (missionLevel()-1)*0.5, 5)`.
    - **Ram X cars** — also absent (no Shield Bump/ram-ability mission exists at all, only jump-ability ones). Needs a per-run ram-kill counter — the same tracking the "Road Rage"/"Chain Reaction" achievement ideas already need, so this shares infrastructure with that work rather than duplicating it. Suggested goal, matching `jump_over_cars`'s own shape: `8 + Math.floor(missionLevel()/5)`.
    - **Witness 2-3 NPC-vs-NPC car crashes** — needs the same new "witnessed" flag the "Not My Fault" achievement idea already needs, shared work. Suggested goal, matching the similarly-rare `jump_ambulance` shape: `1 + Math.floor(missionLevel()/25)`.
    - **Pass through X distinct biomes** (not necessarily all of them — a scaled-down daily version of the "Adventuring Time" achievement, which asks for all 14) — needs the same new per-run biome-set tracking that achievement idea needs, shared work. Suggested goal: 3-5 distinct biomes, scaling gently with level.
  - Not yet implemented — numbers/scoping only, matches how the rest of this pass has gone.

## ENERGY BAR USAGE — mission DONE (2026-09-11), achievement explicitly dropped
Daily Mission `use_bars` implemented: "USE X ENERGY BARS", base 100 +2/level rounded to nearest 5 (matches `pass_cars`'s exact formula shape). New `barsUsedThisRun`/`allTimeStats.totalBarsUsed` counter, incremented on JUMP and Tank-shot activation. Shield Bump ram ALSO counts (direct correction, Batch 374) — but net of its own kill refund, not a flat exclusion: ram activation is +1, a ram-kill's energy refund subtracts that same fraction back out, clamped at 0 when folded in — so a heavy ram chain still can't cheaply farm the mission. A matching TIERED achievement was floated but explicitly declined by direct call ("not very important... looking for something that it's not necessarily a good idea") — `allTimeStats.totalBarsUsed` is tracked and available if this changes later, but no achievement entry exists for it.

## ONE-TIME → TIERED CONVERSIONS — DONE (2026-09-11)
4 former ONE-TIME achievements converted to TIERED with fewer tiers than the main roster (direct request, tier count/spacing left to my judgment): each keeps its OLD one-time tier as the new ceiling — `full_palette`/`car_fanatic`/`i_am_speed` were 'gold' → 3 tiers (bronze/silver/gold); `high_roller` was 'diamond' → 4 tiers (bronze/silver/gold/diamond). New persisted stat added for `i_am_speed` (`allTimeStats.bestTopSpeedKmh` — no lifetime-best existed before, just a live >=250 check). `car_fanatic`'s `v` is derived from the existing `allTimeStats.carStats` map (count of cars with ≥1 run) rather than a dedicated counter. Also fixed a latent bug this surfaced: `achFullNum()` used `Math.round()`, which would have silently broken `high_roller`'s fractional tiers (4.5/7.5) — first tiered achievement with non-integer thresholds.

## JUMP THRUSTER FLAMES FOR NON-STOCK CARS — DONE (2026-09-08, Batch 284)
Generalized to every jump-ability car via a uniform-scale wrapper around the existing stock flame art (`drawFlameFxScaled`), not hand-adapted per car — sidesteps the "~50 cars of real per-car art" concern raised when this was deferred at Batch 151. Cars too narrow for side boosters (hitboxW<12) are forced onto Center Thruster only, free. See CHANGELOG Batch 284.

## SCREEN-SHAPE CONSISTENCY — open design question, not yet decided (2026-09-04)
Direct observation: Menu/Game share one sizing system (`sizeFrame()`/`.canvas-container`, locked to the canvas's own aspect ratio — always reads as a narrow "phone" shape). Every other screen (Setup/Garage/Stats/Daily Gift/Missions/Leaderboard) uses a totally separate system — a centered `.settings-panel` with its own per-view `max-width` (340px base, Stats 506px, Daily Gift/Missions 880px) — not tied to Menu's shape at all, so on a wide window those screens stretch out to their own cap while Menu stays narrow. Explicitly NOT to be implemented yet — thinking-through only, revisit once a direction is picked.
- [ ] **Desktop consistency**: real design decision needed — keep one consistent phone-shaped identity everywhere (cap every screen near Menu's width, wastes space on wide monitors) vs. let content-heavy screens (Missions/Daily Gift's 2-column layouts) stay wider than Menu but on a shared, less extreme cap than the current 880px outlier.
- [x] **Narrow/mobile width — verified ALREADY working (2026-09-08)**: checked the actual CSS (`.msn-grid`/`.dg-grid` both use `grid-template-columns: repeat(auto-fit, minmax(292px, 1fr))`) and confirmed live at a real 375px mobile viewport — both columns already collapse to a single stacked column with no code change needed. This item's old framing ("needs a CSS breakpoint") was stale; nothing left to do here.
- [ ] **Explicit recommendation on the "should things shrink smaller on mobile" question**: no — reflow (stack the columns) instead of scale (shrink text/elements uniformly). Scaling is right for something like the HUD overlay (nothing there needs close reading), but Stats/Missions/Daily Gift are already dense text — shrinking further risks becoming unreadable, which is exactly what the user flagged as a concern. Rearranging layout at the same font size avoids that tradeoff entirely.

## TIERED ACHIEVEMENT PROGRESS — CROSS-RUN CARRYOVER vs. RESET — DONE (2026-09-13, Batch 392)
Decided: option (B), reset per tier — but only for genuinely cumulative stats. Direct rule from the user: "things that are cumulative should reset" (score/coins/overtakes/etc. — repeated in-run gains piling up), while "one time tiered things should not reset" (best-ever peak values like HIGH ROLLER/I AM SPEED) and plain collection tallies (FULL PALETTE/CAR FANATIC/COLLECTOR — "if it's collecting something there's no point in resetting") stay as raw absolute numbers. Implemented via a new `tierReset` flag on 7 of the 12 tiered achievements, applied consistently on both the Achievements tab and the results screen. See CHANGELOG Batch 392 for the exact formula and live verification.

## ACHIEVEMENTS TAB — SETTINGS GEAR ICON — DONE (2026-09-16, Batch 413)
Specification finally given: read as a real gear, sharper, and no box/frame around it. All three done — redrawn as an octagonal hub with 8 teeth and an octagonal bore, viewBox corrected from 14 units at 16px (a blurring 1.143x scale) to a 1:1 16 units, and the button's background/border removed while keeping its 34px hit area. See CHANGELOG Batch 413.

## Original note (2026-09-13), for context
Original: *"better setting icon in garage"*, corrected by the user to mean the **Achievements tab**, not Garage. Refers to the small pixel-art gear icon (`achGearSvg()`) on the `.ach-settings-btn` in the Achievements header, which opens the number-display settings panel (abbreviate numbers / decimal digits / show % above bars). Currently an 8-tooth pixel gear built from plain `<rect>`s. No specifics given on what reads as wrong with it (size, shape, contrast, etc.) — needs a direct follow-up on what "better" means before touching it.

## STATS / LEADERBOARD UX
- [x] **Leaderboard BACK button — DONE (2026-09-08, Batch 275)**: reused the Garage screen's `.garage-panel`/`.garage-scroll` pattern — only the run list scrolls now, BACK stays fixed. See CHANGELOG Batch 275.

## Small tuning leftovers (not big enough for their own batch)
- [x] **DONE (2026-09-21) — all three exist: STAYING ALIVE (tiered minutes), ROAD RAGE (tiered ram kills), and the closest read of "no abilities used" is the secret DIDN'T EVEN TRY. The wider achievement system shipped across Batches 392-438.** Original: Achievements — e.g. "Survived 60 seconds", "Rammed 10 cars", "No abilities used" (idea from early planning, never scoped further). Direct note (2026-09-03): "we'll do achievements later" — deliberately deferred, not dropped.

---

## VISUAL / PRESENTATION ONLY (no mechanical impact on the game — do these last)

## BATCH 21 — Vehicle Visual Variety: Models + Colors
Direct note (2026-09-03): the user can design more car LOOKS but doesn't want to take on more CODE complexity — new full models mean new sprite art plus real integration work (hitbox, rarity tier, spawn weight, Garage card, etc.), while palette/recolor variety is close to free using bodies that already exist. Recommend prioritizing the color-variety items below over new full models for that reason, unless a specific new model is something the user actively wants to design.
- [x] **NPC colors + two-tone trucks — DONE (2026-09-08, Batch 283)**: real-world palette (black/white/grey/navy/red, no pink/lavender/mint/purple); trucks roll an independent white/grey/black trailer color. See CHANGELOG Batch 283.
- [ ] Truck: show front-facing cab (headlights toward player); car variants: taxi, compact, sedan details
- [ ] Also cover player car models, not just NPC traffic — more body-shape options for the player to pick, same spirit as the existing car color picker

## BATCH 22 — Music Overhaul
- [x] Softer gameplay music; multiple tracks; optional speed-scaling BPM; chill menu/pause music — all done (Batches 297-300).
- [x] **Repetition fixed (2026-09-20, Batch 420)**: 7 gameplay tracks (was 3, originals untouched), two new keys, tracks may run 64 steps, and the track now ROTATES every ~30s mid-run instead of being picked once and looped for the whole run. Menu pad got 3 rotating phrases. See CHANGELOG Batch 420.
- [ ] Still open, deliberately not done in Batch 420 (would each be their own pass): per-biome or per-difficulty track selection rather than pure random rotation; a real intro/outro transition between tracks instead of switching cleanly at a loop boundary; drum-sound variety (every track shares the one kick and one hi-hat synth).

---

## MOBILE (deferred — not batches, just a holding area)
User is planning a from-scratch native Android app rebuild later, treating this HTML version purely as a prototype/proof of concept. **Do not implement anything in this section without being asked explicitly** — no work should be invested in mobile-specific behavior here in the meantime.

- [ ] Touch-drag on mobile should feel natural (no lag/jump) — follows the finger 1:1 by setting `player.targetX`, but never verified on an actual touch device
- [ ] Test on portrait and landscape orientations — never done
- [ ] Tilt steering sets `player.targetX` proportionally to tilt angle (matches the pre-Batch-41 slider behavior) — never redesigned or tuned properly, since mobile input is out of scope until the native rebuild

---

## ONLINE LEADERBOARD / SCORE SERVER — stated 2026-09-16, not started
User's goal: a server that saves scores and shows a leaderboard, "for when
the game is ready for solo play." Currently the leaderboard is local only
(top 10 in `localStorage`, filterable by lane count).

Not scoped yet. Open questions that need answering before any build:
- [ ] Is this a global public leaderboard, or just cross-device sync of the
      player's own scores? Changes the design completely.
- [ ] **Anti-cheat is the real problem.** Scores are computed client-side in
      a readable HTML file — anyone can POST a fabricated score. Options,
      cheapest first: (a) accept it, treat the board as fun-not-competitive;
      (b) sanity-bound submissions server-side (max plausible score for a
      given survival time / lane count); (c) submit a replay/input trace and
      re-simulate server-side (expensive, needs deterministic physics).
      Worth deciding EARLY — it constrains the API shape.
- [ ] Connects to the User ID / cross-device portability idea already logged
      in the packaging section above — same identity question, should be
      designed together rather than twice.
- [ ] Hosting: where does this actually run? A server implies ongoing cost
      and uptime, which conflicts with the "no hosting, runs from a local
      file" stance taken in the packaging research above. Needs a call.

Proposed stack if/when it starts (not decided): Node + a small HTTP server
+ SQLite, since Node 22 is already in the workspace and it keeps the
dependency surface near zero. `POST /score`, `GET /leaderboard`.

---

## Notes & Constraints
- Game is a single HTML file — keep it that way unless complexity demands splitting
- No external libraries/frameworks unless absolutely necessary
- Must work on both desktop (keyboard + mouse) and mobile (touch + tilt) for now, but see the Mobile section above — no further mobile investment until the native app rebuild
- Target browsers: Chrome, Firefox, Safari (iOS), Chrome (Android)
- **Steering, current state (2026-08-25, Batch 44/46/48)**: horizontal is fully smooth/continuous while a key is held (glides from the very first frame, no discrete snap first), rounding to the nearest lane center only on release; vertical has acceleration/friction/braking physics — not an instant constant speed. History: lane-snap was the original design (research-backed, genre convention) → Batch 41 removed it in favor of full free movement on both axes → after playtesting, Batch 44 reverted horizontal back to lane-snap (discrete snap-then-glide) because free movement's overshoot/undershoot felt like bad luck rather than a skill issue → Batch 48 simplified further, removing the initial discrete snap so holding a key is smooth from the first frame; vertical stayed free since that was never the complaint, then got its own physics rework in Batch 46. Don't remove the lane-center-on-release behavior without being asked — free movement (never rounding to a lane) has been tried and rejected once already.
- **Lane-change banking tilt was removed entirely (Batch 210, 2026-09-06)** — direct request, it caused a visible render-path discontinuity on the tank specifically. Don't re-add cosmetic rotation to the lane-change glide without being asked; the glide itself (`this.x` easing to `this.targetX`) is unaffected and still the same speed/curve as before.
- **Night mode was built out then removed entirely (Batch 188, 2026-09-03)** — ambient overlay → real front/rear vehicle lights → cone headlights → several polish rounds, then scrapped per direct decision once matching per-car taillight positions across 50+ sprites became a bigger ask than wanted: "i would like each car, so maybe this night mode is not a good idea? you know what that enough, scrap whole night mode, remove everything associated with it." The underlying lighting mechanics worked and were well-received — what killed it was chasing pixel-perfect fidelity across the whole existing roster, not the core concept. Don't re-propose without being asked; if picked up again, budget real time for per-sprite taillight work up front.
